// =============================================================================
// U10 Nakama Server — Runtime Bundle (index.js)
// =============================================================================
// Generated dynamically by build.js
// =============================================================================

// ─────────────────────────────────────────────────────────────────────────────
// LEGAL DEFAULTS
// ─────────────────────────────────────────────────────────────────────────────
// Automatically generated from U10_Legal_Documents.txt. Do not edit directly.

var DEFAULT_TERMS_OF_SERVICE = `================================================================================
                            TERMS OF SERVICE
================================================================================

1. ACCEPTANCE OF TERMS

These Terms of Service (hereinafter "Terms") constitute a binding agreement 
between you (hereinafter "User," "Player," or "You") and GOB Interactive 
(hereinafter "Company," "We," "Us," or "Our"). By downloading, installing, 
accessing, or playing U10 (hereinafter "the Game" or "the Service"), you 
acknowledge that you have read, understood, and agree to be bound by these 
Terms in their entirety. If you do not agree to these Terms, you are 
expressly prohibited from using the Game.

These Terms apply to all users regardless of jurisdiction, but are subject to 
the laws of the Republic of India.


2. ELIGIBILITY AND AGE REQUIREMENT

2.1 You must be at least 13 years of age to access and use the Game, or such 
higher age as required by the laws of your country of residence.

2.2 If you are under 18 years of age, you represent and warrant that you have 
obtained valid consent from your parent, guardian, or legal representative 
before accessing the Game.

2.3 If you are a parent or guardian allowing a minor to use the Game, you 
assume full responsibility for monitoring their use and compliance with these 
Terms.

2.4 We reserve the right to verify your age at any time. Providing false age 
information or allowing unauthorized access constitutes a material breach of 
these Terms.


3. ACCOUNT REGISTRATION AND SECURITY

3.1 To access certain features of the Game, you may be required to create an 
account using Google Sign-In authentication or other authorized methods.

3.2 You agree to provide accurate, complete, and truthful information during 
registration. You are solely responsible for maintaining the confidentiality of 
your authentication credentials, account tokens, and login information.

3.3 Any activity conducted under your account is your sole responsibility. You 
agree not to share your account credentials with third parties or allow 
unauthorized access to your account.

3.4 You must immediately notify us of any unauthorized access, security breach, 
or suspicious activity related to your account.

3.5 We are not liable for any losses, damages, or unauthorized transactions 
resulting from your failure to maintain account security or from your disclosure 
of credentials to third parties.


4. NATURE OF THE GAME - PURE FUN AND ENTERTAINMENT

4.1 U10 is a casual multiplayer card game designed solely for entertainment, 
amusement, and recreational purposes.

4.2 IMPORTANT: U10 is NOT a gambling game. The virtual currency system 
(Coins, Stakes, Shields) exists purely to create gameplay mechanics, enable 
matchmaking tiers, and provide progression systems. These virtual elements have 
NO REAL MONETARY VALUE and CANNOT be converted to real money or legal tender.

4.3 The stake system in U10 is a matchmaking mechanism only. Players use 
virtual coins to enter matches of varying difficulty levels. This system is 
designed to pair players of similar skill levels and add competitive tension, 
NOT to simulate gambling or enable wagering of real assets.

4.4 Participation in the Game is entirely voluntary and for entertainment 
purposes. No user is required to spend real money to progress or enjoy core 
gameplay features.

4.5 The Game complies with all applicable gaming regulations in India and does 
not constitute gambling, betting, or any activity regulated under the Public 
Gambling Act, 1867, or related legislation.


5. VIRTUAL CURRENCY AND VIRTUAL ASSETS

5.1 The Game contains the following virtual elements: Coins (c), Arena Shields, 
Spin Tokens, XP Boosts, Achievements, Seasonal Experience Points (SXP), and 
Cosmetic Skins.

5.2 All virtual currency and assets are the exclusive property of GOB 
Interactive. You are granted a limited, non-exclusive, non-transferable, 
revocable license to use these items solely within the Game.

5.3 Virtual currency and assets:
    - Possess NO real-world monetary value
    - Cannot be exchanged, sold, or transferred for real money or legal tender
    - Cannot be traded to other players or third parties
    - Exist only within the Game environment
    - Are subject to forfeiture upon account termination or Game closure

5.4 We reserve the right to modify, adjust, redistribute, or remove virtual 
currency and assets at any time for game balance, fraud prevention, or 
operational reasons. Such actions do not constitute breach of contract or 
compensation obligation.

5.5 STRICTLY PROHIBITED: 
    - Selling, trading, or transferring virtual currency for real money
    - Using virtual currency for gambling, wagering, or monetization
    - Trading virtual items on third-party platforms
    - Representing virtual currency as having real-world value

Violation of these prohibitions will result in immediate permanent account 
suspension without refund or compensation.


6. PROHIBITED CONDUCT

You agree not to engage in, attempt, or facilitate any of the following:

6.1 CHEATING AND EXPLOITATION:
    - Using cheat engines, memory editors, or packet sniffers
    - Reverse-engineering, decompiling, or analyzing game binaries
    - Exploiting bugs, glitches, or unintended mechanics for unfair advantage
    - Using automated scripts, bots, or macros to automate gameplay
    - Speed hacking, lag switching, or manipulating network connections
    - Injecting malicious code into game processes or communications

6.2 ACCOUNT FRAUD:
    - Creating multiple accounts to gain unfair advantage
    - Account sharing, selling, or trading accounts
    - Impersonating other players or creating fake identities
    - Unauthorized access to other users' accounts
    - Obtaining unauthorized rewards, currency, or items

6.3 SERVER AND DATA MANIPULATION:
    - Forging or tampering with API requests or server communications
    - Submitting false match results or manipulated game data
    - Attempting to claim unauthorized rewards or currency
    - Intercepting or modifying secure communications with our servers
    - Distributing modified or hacked versions of the Game

6.4 HARASSMENT AND MISCONDUCT:
    - Harassing, threatening, abusing, or defaming other players
    - Discrimination based on protected characteristics
    - Spamming or flooding in-game communication channels
    - Sharing personal information of others without consent
    - Attempting to cause physical, emotional, or financial harm to others

6.5 ILLEGAL ACTIVITY:
    - Using the Game for illegal purposes or fraud
    - Attempting unauthorized transactions or chargebacks
    - Violating intellectual property or privacy rights
    - Any conduct violating applicable laws of India or user's jurisdiction


7. CONSEQUENCES OF VIOLATION

7.1 We employ advanced anti-cheat systems and monitoring to detect violations. 
Upon discovery of any breach:

7.2 IMMEDIATE ACTIONS:
    - Temporary or permanent account suspension
    - Deletion of virtual currency and assets
    - IP address blocking
    - Data retention for investigation and law enforcement

7.3 PERMANENT BAN:
    - Violation of sections 6.1, 6.2, or 6.3 results in permanent, irreversible 
      account termination
    - Associated accounts and devices may be banned
    - No refund of any purchases or virtual items
    - Legal action may be pursued under applicable laws

7.4 We do not provide warnings before account termination for severe violations. 
We reserve the right to exercise our sole discretion in determining the 
severity of violations and appropriate penalties.


8. INTELLECTUAL PROPERTY RIGHTS

8.1 The Game, including all software, source code, graphics, audio, animations, 
game mechanics, story elements, user interface, and server infrastructure, are 
the exclusive intellectual property of GOB Interactive.

8.2 You are not granted any right to:
    - Modify, adapt, translate, or create derivative works
    - Decompile, disassemble, or reverse engineer any portion
    - Redistribute, sell, rent, or lease the Game or components
    - Create unauthorized copies or backup versions beyond personal use
    - Use any Game content for commercial purposes

8.3 The Game may reference or be inspired by traditional card game mechanics, 
but all original implementation, code, and design are proprietary to GOB 
Interactive.

8.4 You may take screenshots or video recordings for personal, non-commercial 
sharing (e.g., social media, streaming) provided you include appropriate 
attribution to GOB Interactive and comply with these Terms.


9. SERVICE AVAILABILITY AND MODIFICATIONS

9.1 The Game is provided on an "AS-IS" and "AS-AVAILABLE" basis. We make no 
guarantees regarding:
    - Uninterrupted service availability
    - Absence of errors, bugs, or performance issues
    - Data persistence or backup
    - Specific performance metrics or latency

9.2 We reserve the right to:
    - Modify, update, or patch the Game at any time
    - Change game mechanics, balance, or features without notice
    - Adjust virtual currency exchange rates or reward systems
    - Alter matchmaking algorithms or ranking systems
    - Discontinue features or services with notice

9.3 We may conduct scheduled or emergency maintenance that temporarily 
interrupts service. We will attempt to provide advance notice, but emergency 
maintenance may proceed without warning.

9.4 We are not responsible for:
    - Loss of progress due to server failures, crashes, or data loss
    - Forfeiture of virtual currency or items due to technical issues
    - Network connectivity problems on your device or ISP
    - Device malfunctions, incompatibility, or lack of storage space
    - Deletion of app data due to device reset or app reinstallation


10. LIMITATION OF LIABILITY

10.1 TO THE MAXIMUM EXTENT PERMITTED BY LAW, GOB INTERACTIVE SHALL NOT BE 
LIABLE FOR:
    - Any indirect, incidental, special, consequential, or punitive damages
    - Loss of data, progress, virtual currency, or items
    - Loss of revenue, profit, or business opportunity
    - Emotional distress or reputational harm
    - Third-party claims or actions

10.2 Our total liability under these Terms shall not exceed the amount you have 
paid (if any) in the preceding 12 months for access to the Game.

10.3 Some jurisdictions do not permit limitation of liability. If such laws 
apply to you, the above limitations may not apply in full.


11. TERMINATION

11.1 Your right to use the Game terminates immediately upon:
    - Breach of these Terms or the Privacy Policy or EULA
    - Violation of applicable laws or Game policies
    - Expiration of any license term we may establish
    - Our decision to discontinue the Game or your service

11.2 Upon termination:
    - Your account and all associated data will be deleted
    - Your virtual currency and items will be forfeited
    - You must cease all use of the Game
    - Certain provisions (IP rights, limitation of liability, governing law) 
      survive termination

11.3 Termination does not obligate us to provide notice, opportunity to appeal, 
or compensation.


12. DISPUTE RESOLUTION AND GOVERNING LAW

12.1 These Terms are governed by and construed in accordance with the laws of 
the Republic of India, without regard to conflicts of law principles.


12.2 You agree to submit to the personal jurisdiction of such courts and waive 
any objection to venue or inconvenient forum.

12.3 If you are accessing the Game from outside India, you acknowledge that 
these Terms may be subject to the laws of your jurisdiction, and you are 
responsible for complying with local laws.


13. AMENDMENTS AND MODIFICATIONS

13.1 We may amend these Terms at any time by posting the revised version on our 
website or within the Game.

13.2 Your continued use of the Game following publication of revised Terms 
constitutes your acceptance of the changes.

13.3 We will make reasonable efforts to notify users of material changes through 
in-game messages or other means.


14. SEVERABILITY

If any provision of these Terms is found to be invalid, illegal, or 
unenforceable, the remaining provisions shall continue in full force and 
effect.


================================================================================`;

var DEFAULT_PRIVACY_POLICY = `PRIVACY POLICY
================================================================================

1. INTRODUCTION AND COMMITMENT TO PRIVACY

GOB Interactive ("We," "Us," "Our," or "Company") is committed to protecting 
the privacy and security of your personal information. This Privacy Policy 
explains what information we collect, how we use it, who we share it with, and 
what rights you have regarding your data.

This Privacy Policy applies to the U10 mobile game and all associated services, 
including the Nakama backend infrastructure. By using the Game, you consent to 
the practices described herein.

This Privacy Policy is governed by the laws of the Republic of India and 
complies with the Information Technology Act, 2000, and applicable regulations.


2. INFORMATION WE COLLECT

2.1 AUTHENTICATION AND PROFILE INFORMATION:

When you sign in using Google Sign-In, we collect and store:
    - Your unique Google Account ID (a non-sensitive identifier)
    - Your display name or nickname as provided by Google
    - Your Google profile picture URL
    - Your registered email address
    - Your authentication token for session management

We DO NOT:
    - Access or store your Google account password
    - Request access to Gmail, Google Drive, or other Google services
    - Collect personally identifiable information beyond what Google provides
    - Store any financial or payment information


2.2 GAMEPLAY AND TELEMETRY DATA:

To provide a fair, balanced, and secure gaming experience, we collect and store:
    - Match history (opponents, results, duration)
    - Your game statistics (wins, losses, win rate)
    - Your current rank, tier, or skill rating
    - Experience points (XP) and seasonal progression
    - Virtual currency balance (Coins, Shields, Tokens)
    - Cosmetic items purchased, earned, or unlocked
    - Achievement progress and completion status
    - Leaderboard ranking and position
    - Daily login streaks and reward claims
    - Weekly quest progress and completion
    - Spin wheel interactions and rewards claimed
    - Session logs (login times, session duration)

This data is stored on our secure Nakama backend servers and is essential for:
    - Matchmaking and skill-based ranking
    - Preventing fraud and detecting cheating
    - Calculating rewards and maintaining game economy
    - Leaderboard generation and competitive integrity
    - Personalizing your gaming experience


2.3 DEVICE AND SYSTEM INFORMATION:

To optimize performance and compatibility, we collect:
    - Device model and manufacturer
    - Operating system version (Android version)
    - Device unique identifier (but NOT IMEI)
    - IP address (for security and matchmaking)
    - Network type (WiFi, cellular, etc.)
    - Approximate geographic location (country, city level)
    - Device storage available
    - Crash reports and error logs
    - App version you are running
    - Screen resolution and display metrics


2.4 ADVERTISING AND OPTIONAL INFORMATION:

If you choose to watch rewarded video advertisements:
    - We integrate with Google AdMob or similar advertising platforms
    - These platforms may collect:
        • Google Advertising ID (AD_ID) - this is an optional, resettable identifier
        • Coarse location (country or city)
        • Device type and OS version
        • Advertising preferences
    
    - Watching ads is OPTIONAL. Declining ads does not prevent gameplay.
    - You can reset your advertising ID at any time in device settings
    - Advertising partners operate under their own privacy policies


2.5 CRASH REPORTS AND ANALYTICS:

To improve stability and user experience:
    - We automatically collect crash reports and error logs
    - These may include stack traces, memory usage, and device state
    - Analytics tools (e.g., Firebase Analytics, AppsFlyer) track:
        • User retention and engagement metrics
        • Feature usage patterns
        • Performance metrics
        • Conversion funnels


2.6 INFORMATION YOU VOLUNTARILY PROVIDE:

    - In-game chat or communication messages
    - Support or feedback submitted through the Game
    - Information provided during account recovery
    - Any content you create or share in public leaderboards/profiles


3. HOW WE USE YOUR INFORMATION

3.1 PRIMARY PURPOSES:

    - Authentication: Securely verifying your identity
    - Service Delivery: Enabling gameplay, matchmaking, and game mechanics
    - Fair Play: Detecting and preventing cheating, hacking, and fraud
    - Leaderboards: Generating global rankings and competitions
    - Economy Management: Calculating rewards, preventing duplicate claims
    - Technical Support: Diagnosing issues and improving stability
    - Security: Monitoring for unauthorized access or suspicious activity


3.2 ANALYTICS AND IMPROVEMENT:

    - Understanding player behavior and engagement
    - Optimizing game balance and matchmaking
    - Analyzing feature usage to improve design
    - Identifying technical issues and bugs
    - Measuring business performance and retention


3.3 COMMUNICATIONS:

    - Sending important updates about the Game
    - Notifications about rewards, achievements, or seasonal events
    - Technical alerts about maintenance or service issues
    - Responses to your support requests

You can disable non-essential push notifications in your device settings.


3.4 LEGAL AND COMPLIANCE:

    - Complying with court orders or legal requests from Indian authorities
    - Investigating violations of these Terms or applicable laws
    - Protecting our legal rights and intellectual property
    - Enforcing this Privacy Policy and other agreements


4. DATA SHARING AND THIRD PARTIES

4.1 WHO WE DO NOT SHARE DATA WITH:

We DO NOT:
    - Sell your personal data to third-party data brokers
    - Share your personal information for marketing purposes
    - Lease or rent your data to other companies
    - Share your data with unrelated third parties without your consent
    - Use your data for purposes other than those listed above


4.2 AUTHORIZED SERVICE PROVIDERS:

We share LIMITED information with the following service providers who help us 
operate the Game:

    - NAKAMA SERVER BACKEND: Stores core gameplay data, accounts, and inventory
      (operated by Heroic Labs or our managed instance)
    - GOOGLE CLOUD INFRASTRUCTURE: Hosting and data storage
    - GOOGLE PLAY SERVICES: Authentication, payment processing, and analytics
    - GOOGLE ADMOB: Serving optional rewarded advertisements
    - CRASH REPORTING: Firebase Crashlytics or similar (crash logs only)
    - ANALYTICS PLATFORMS: Aggregate usage statistics (not personal data)

These service providers:
    - Are bound by data processing agreements
    - Have access only to data necessary for their function
    - Are prohibited from using your data for their own purposes
    - Must comply with applicable data protection laws


4.3 LEGAL REQUIREMENTS:

We may disclose your information when required by law:
    - Court orders or legal subpoenas from Indian courts
    - Government requests from legitimate authorities
    - Prevention of illegal activity or fraud
    - Protection of our legal rights or safety
    - In such cases, we will provide you notice unless legally prohibited


4.4 BUSINESS TRANSFERS:

In the event of merger, acquisition, bankruptcy, or similar transaction:
    - Your data may be transferred as part of the transaction
    - You will be notified of any material changes to this Privacy Policy
    - You may opt out of any new uses by requesting deletion


5. DATA RETENTION AND STORAGE

5.1 How long we keep your data:

    - ACTIVE ACCOUNTS: Data is retained as long as your account is active
    - DELETED ACCOUNTS: Data is permanently deleted within 7 business days of 
      deletion request
    - CRASH LOGS: Retained for 90 days for debugging purposes
    - ANALYTICS DATA: Aggregated data retained indefinitely; personal data 
      removed after 12 months
    - LEGAL HOLDS: Data retained if required for legal proceedings


5.2 Storage Location:

    - Primary storage: Google Cloud Infrastructure (secured data centers)
    - Backup servers: Geographically distributed for redundancy
    - All servers employ encryption at rest and in transit


5.3 Data Security Measures:

    - ENCRYPTION: All data in transit uses TLS 1.2+ encryption
    - AUTHENTICATION: Role-based access control for internal systems
    - MONITORING: Continuous monitoring for unauthorized access attempts
    - BACKUPS: Regular encrypted backups with restricted access
    - INCIDENT RESPONSE: Rapid response team for security breaches


6. YOUR PRIVACY RIGHTS AND CHOICES

6.1 RIGHT TO ACCESS YOUR DATA:

You have the right to request a copy of all personal data we hold about you, 
including:
    - Your profile information
    - Gameplay statistics and history
    - Account data and virtual items
    - All data collected about your device and activity

To request access:
    - Use the "Request My Data" feature in Game Settings (if available), or
    - Contact: [support contact to be provided by user]
    
We will provide this information within 30 days in a readable format.


6.2 RIGHT TO DATA DELETION (RIGHT TO BE FORGOTTEN):

You have the right to request complete deletion of your account and all 
associated data from our servers.

Upon deletion:
    - Your personal profile information will be permanently removed
    - All gameplay statistics and history will be deleted
    - Virtual currency and items will be forfeited
    - Your account will be deactivated and cannot be recovered
    - This action is IRREVERSIBLE

To request deletion:
    - Use the "Delete My Account" feature in Game Settings, or
    - Contact: [support contact to be provided by user]

Processing Timeline:
    - Request confirmation: Within 3-5 business days
    - Complete deletion: Within 7 business days
    - We will send confirmation when deletion is complete


6.3 RIGHT TO DATA CORRECTION:

You have the right to correct inaccurate personal data. For example:
    - Updating your display name
    - Correcting profile information
    - Requesting correction of data in your account

Use the Game settings to update your information directly, or contact us for 
assistance.


6.4 RIGHT TO OBJECT:

You have the right to object to certain uses of your data:
    - You may opt out of non-essential push notifications
    - You may refuse optional analytics collection
    - You may disable interest-based advertising (see your device settings)

Opting out of these does not affect core gameplay functionality.


6.5 RIGHT TO DATA PORTABILITY:

You have the right to receive your data in a structured, machine-readable 
format (CSV, JSON, etc.) and transfer it to another service provider.

To request your data in portable format:
    - Contact: [support contact to be provided by user]

We will provide this within 30 days at no cost.


6.6 EXERCISING YOUR RIGHTS:

For any privacy-related requests:
    - Submit through in-game "Settings" menu, or
    - Email: [support contact to be provided by user]
    - Include: Your account email, account ID, and specific request

We will verify your identity before processing sensitive requests.

All requests will be processed without unreasonable delay and at no cost to you 
(except where permitted by law for manifestly unfounded or excessive requests).


7. CHILDREN'S PRIVACY (UNDER 18)

7.1 The Game is directed at users 13 and older. If you are under 13, do not 
use the Game.

7.2 For users between 13-18 years old:
    - We collect minimal personal information
    - We do not use behavioral data for targeted advertising
    - We do not share your data with third-party marketers
    - Parents/guardians may request deletion of minor's account

7.3 Parents/Guardians:
    - You are responsible for monitoring your child's Game usage
    - You may request deletion of your child's account and data
    - Contact us with proof of parental/guardian status


8. CALIFORNIA AND INTERNATIONAL USERS

8.1 CCPA COMPLIANCE (If you are in California):

If you are a California resident, you have additional rights under the California 
Consumer Privacy Act (CCPA):
    - Right to know what data is collected
    - Right to delete data
    - Right to opt-out of data sales (we do not sell data)
    - Right to non-discrimination for exercising rights

These rights are honored in addition to rights provided elsewhere in this 
Privacy Policy.


8.2 GDPR COMPLIANCE (If you are in the EU):

If you are in the European Union, your data is protected under GDPR:
    - We process data based on your consent and legitimate interests
    - You have additional rights including objection and restriction
    - You may lodge complaints with your data protection authority
    - We have appointed a Data Protection Officer (contact via support)


8.3 INDIA-SPECIFIC RIGHTS:

As this Privacy Policy is primarily governed by Indian law:
    - Your rights are protected under the Information Technology Act, 2000
    - You may file complaints with the Data Protection Authority
    - Data will be stored within India to the extent feasible
    - Your rights to access and deletion are provided above


9. THIRD-PARTY LINKS AND SERVICES

The Game may contain links to third-party websites, social media, or services. 
We are not responsible for their privacy practices.

When you access external links:
    - You are subject to their privacy policies
    - We recommend reviewing their privacy statements
    - We do not control their data practices
    - Your data sharing with them is your responsibility


10. SECURITY MEASURES

10.1 We employ industry-standard security practices:

    - Data Encryption: TLS 1.2+ for data in transit; AES-256 for data at rest
    - Access Control: Role-based access; minimum privilege principle
    - Authentication: Multi-factor authentication for admin accounts
    - Monitoring: Real-time intrusion detection and threat monitoring
    - Regular Audits: Third-party security assessments and penetration testing
    - Incident Response: Documented procedures for security breaches
    - Employee Training: Regular security awareness training


10.2 While we implement robust security measures, no system is 100% secure. We 
cannot guarantee absolute protection against sophisticated attacks.

10.3 If a security breach occurs that compromises your data:
    - We will investigate promptly
    - We will notify affected users within 72 hours
    - We will cooperate with law enforcement as required
    - We will take corrective action to prevent recurrence


11. DATA PROCESSING AGREEMENT (FOR BUSINESS PARTNERS)

If you are a business partner or operating on behalf of GOB Interactive, 
additional Data Processing Agreements may apply. Contact us for details.


12. AMENDMENTS TO PRIVACY POLICY

12.1 We may update this Privacy Policy to reflect changes in our practices, 
technology, or legal requirements.

12.2 We will post the updated policy on our website and within the Game with a 
revised "Last Updated" date.

12.3 Material changes will be communicated via in-game notification or email.

12.4 Your continued use of the Game constitutes acceptance of the updated 
Privacy Policy.


13. CONTACT US FOR PRIVACY INQUIRIES

For any questions about this Privacy Policy or to exercise your privacy rights:

[Company will provide contact details]

We will respond to all inquiries within 15 business days.


================================================================================`;

var DEFAULT_EULA = `END USER LICENSE AGREEMENT (EULA)
================================================================================

1. GRANT OF LICENSE

1.1 This End User License Agreement ("EULA") is a binding legal contract between 
you ("Licensee," "User," or "You") and GOB Interactive ("Licensor," "Company," 
"We," or "Us").

1.2 Subject to your strict compliance with this EULA, the Terms of Service, and 
the Privacy Policy, Licensor grants you a personal, non-exclusive, 
non-transferable, non-sublicensable, revocable, and limited license to:

    - Download the U10 game client from Google Play Store
    - Install the Game on compatible Android devices you own or control
    - Execute and run the Game solely for your private, personal, 
      non-commercial entertainment purposes
    - Access the Game's servers and participate in multiplayer matches

1.3 This license is personal to you and may not be assigned, transferred, sold, 
or shared with others.

1.4 This license is revocable at any time if you violate these terms or engage 
in prohibited conduct.


2. SCOPE OF USE RIGHTS

2.1 PERMITTED USES:

You may:
    - Play the Game for personal entertainment
    - Create gameplay content (screenshots, videos) for personal, non-commercial 
      sharing
    - Stream the Game on platforms (YouTube, Twitch) provided you include 
      attribution to GOB Interactive
    - Share your gameplay experiences with friends
    - Participate in community events and leaderboards


2.2 STRICTLY PROHIBITED USES:

You may NOT:
    - Sell, rent, lease, or trade the Game or any part of it
    - Distribute copies of the Game outside official channels
    - Create a "backup" or secondary copy for any purpose
    - Share your account credentials with others
    - Allow others to play using your account
    - Use the Game for commercial purposes without written permission


3. INTELLECTUAL PROPERTY AND OWNERSHIP

3.1 LICENSOR OWNERSHIP:

All intellectual property rights in the Game are the exclusive property of GOB 
Interactive, including but not limited to:
    - Source code and compiled binaries
    - Game engines and frameworks
    - Graphics, animations, and visual assets
    - Audio, music, and sound design
    - Game mechanics, algorithms, and artificial intelligence
    - Story, narrative, and game world design
    - User interface and user experience design
    - Database schemas and server infrastructure
    - All modifications, updates, and derivatives


3.2 THIRD-PARTY LICENSES:

The Game incorporates third-party software and libraries used under open-source 
or commercial licenses, including (but not limited to):
    - Godot Engine (MIT License)
    - Google Play Services
    - Android SDK
    - Nakama server framework

These components are licensed to you under their respective license terms. 
Copies of open-source licenses are included with the Game.


3.3 YOUR LIMITED OWNERSHIP:

You own the physical copy of the Game on your device, but you do NOT own:
    - The software code or architecture
    - The Game's intellectual property
    - Any modifications or derivative works
    - Your account or virtual items (see Terms of Service)


4. LICENSE RESTRICTIONS

You agree NOT to:

4.1 REVERSE ENGINEERING AND MODIFICATION:
    - Decompile, disassemble, or reverse-engineer the Game or any component
    - Attempt to discover the source code or underlying algorithms
    - Translate, adapt, or create modified versions
    - Create derivative works based on the Game
    - Patch or modify the Game executable or resources

4.2 CIRCUMVENTING PROTECTIONS:
    - Bypass security, anti-tampering, or DRM protections
    - Remove or disable anti-cheat systems
    - Modify game memory or internal state
    - Intercept or spoof network communications
    - Hack or modify the Nakama backend or server infrastructure

4.3 UNAUTHORIZED DISTRIBUTION:
    - Redistribute the Game outside official channels
    - Create or share unauthorized patches or modifications
    - Compile or distribute derivative software
    - Host the Game on unauthorized servers or mirrors

4.4 COMMERCIAL EXPLOITATION:
    - Use the Game for commercial purposes
    - Charge users for access to the Game
    - Use Game assets in commercial products
    - Monetize gameplay footage without proper licensing
    - Create competitive products based on Game mechanics


5. WARRANTY DISCLAIMER

5.1 "AS-IS" BASIS:

THE GAME IS PROVIDED ON AN "AS-IS," "AS-AVAILABLE," AND "WITH ALL FAULTS" 
BASIS. LICENSOR DISCLAIMS ALL WARRANTIES, EXPRESS OR IMPLIED, INCLUDING:

    - WARRANTIES OF MERCHANTABILITY OR FITNESS FOR A PARTICULAR PURPOSE
    - WARRANTIES OF TITLE, NON-INFRINGEMENT, OR QUIET ENJOYMENT
    - WARRANTIES REGARDING RELIABILITY, AVAILABILITY, OR CONTINUOUS SERVICE
    - WARRANTIES THAT THE GAME WILL MEET YOUR EXPECTATIONS OR REQUIREMENTS
    - WARRANTIES THAT DEFECTS WILL BE CORRECTED


5.2 NO GUARANTEE OF:
    - Uninterrupted or error-free service
    - Specific performance metrics or latency
    - Preservation of progress or virtual items
    - Continued availability of features or the Game itself
    - Compatibility with future device updates or OS versions


5.3 Some jurisdictions do not permit disclaimers of implied warranties. To the 
extent required by law, implied warranties are limited to 30 days from initial 
download.


6. LIMITATION OF LIABILITY

6.1 TO THE MAXIMUM EXTENT PERMITTED BY LAW, IN NO EVENT SHALL LICENSOR BE 
LIABLE FOR:

    - Any indirect, incidental, special, consequential, or punitive damages
    - Loss of profits, revenue, data, or business opportunity
    - Loss of virtual currency, items, or game progress
    - Damage to your device or files
    - Interruption of service or server downtime
    - Third-party claims, actions, or demands
    - Any damages arising from your violation of this EULA


6.2 This limitation applies even if Licensor has been advised of the 
possibility of such damages.


6.3 TOTAL LIABILITY CAP:

Licensor's total aggregate liability under this EULA shall not exceed the 
amount you have paid (if any) in the preceding 12 months for access to the 
Game. If you paid nothing, liability is limited to INR 100 (or equivalent in 
your currency).


6.4 JURISDICTIONAL VARIATIONS:

Some jurisdictions (including India) do not permit exclusion or limitation of 
liability for certain damages. To the extent such laws apply, the above 
limitations may be restricted, but Licensor's liability will be minimized to 
the extent legally permissible.


7. INDEMNIFICATION

7.1 You agree to indemnify, defend, and hold harmless GOB Interactive, its 
officers, directors, employees, and agents from any claims, damages, losses, or 
expenses (including legal fees) arising from:

    - Your violation of this EULA, the Terms of Service, or Privacy Policy
    - Your unauthorized use of the Game
    - Your violation of any applicable law
    - Your use of the Game in a manner that infringes third-party rights
    - Your conduct toward other players or our staff


8. TERMINATION

8.1 This EULA is effective from the date you first accept these terms and 
continues until terminated.

8.2 AUTOMATIC TERMINATION:

Your license is automatically terminated immediately if you:
    - Violate any material term of this EULA
    - Violate the Terms of Service or Privacy Policy
    - Engage in cheating, hacking, or prohibited conduct
    - Attempt to reverse-engineer or hack the Game
    - Are found to have exploited the Game unfairly


8.3 LICENSOR TERMINATION:

Licensor may terminate your license at any time by:
    - Notifying you of termination
    - Disabling your account
    - Blocking your device from accessing the Game
    - Terminating the Game entirely (with reasonable notice when practical)


8.4 UPON TERMINATION:

    - Your license to use the Game immediately ceases
    - You must immediately uninstall the Game from all devices
    - Your account and all associated data will be deleted
    - All virtual currency and items will be forfeited
    - You must cease all use of Game materials and assets


8.5 Certain provisions survive termination, including limitations of liability, 
intellectual property, indemnification, and governing law.


9. UPDATES AND MODIFICATIONS

9.1 Licensor may, at its sole discretion:
    - Update, patch, or modify the Game at any time
    - Change game mechanics, balance, or features
    - Modify server-side systems or backend infrastructure
    - Alter or discontinue features or services
    - Require you to download and install updates to continue playing


9.2 You acknowledge that:
    - Updates may change gameplay significantly
    - Some updates may introduce new bugs or issues
    - Updates are provided at no additional cost
    - Refusal to update may prevent you from playing


9.3 Licensor is not liable for any issues arising from updates, including loss 
of progress, incompatibility, or reduced performance.


10. PRIVACY AND DATA

10.1 Your use of the Game is subject to the separate Privacy Policy, which 
details how your personal data and gameplay information are collected and used.

10.2 By accepting this EULA, you also accept the Privacy Policy.

10.3 Refer to the Privacy Policy for:
    - What data is collected
    - How data is used and shared
    - Your privacy rights and how to exercise them
    - Data deletion and retention policies


11. THIRD-PARTY CONTENT AND SERVICES

11.1 The Game may include links to or integrate with third-party services:
    - Google Play Services
    - Social media platforms
    - Analytics or advertising services
    - Cloud storage services

11.2 These third-party services are governed by their own terms and privacy 
policies. Licensor is not responsible for:
    - Their availability or functionality
    - Your use of their services
    - Their data practices or privacy policies
    - Any issues arising from their services


12. COMPLIANCE WITH APPLICABLE LAWS

12.1 You agree to comply with all applicable laws and regulations in your 
jurisdiction, including:
    - The Information Technology Act, 2000 (India)
    - The Public Gambling Act, 1867 (India)
    - Any gaming regulations or restrictions
    - Export control laws (if applicable)


12.2 The Game is not available in jurisdictions where it is prohibited by law. 
You are responsible for verifying its legality in your jurisdiction.

12.3 If the Game is declared illegal in your jurisdiction, Licensor may 
terminate your license without refund or compensation.


13. DISPUTES AND GOVERNING LAW

13.1 GOVERNING LAW:

This EULA is governed by and construed in accordance with the laws of the 
Republic of India, without regard to conflict of law principles.


13.2 JURISDICTION:

Any dispute, claim, or controversy arising from this EULA shall be subject to 
the exclusive jurisdiction of courts located in Madurai, Tamil Nadu, India.


13.3 DISPUTE RESOLUTION:

Before initiating legal action:
    - First, attempt to resolve disputes through good-faith negotiation
    - Provide written notice of the dispute to Licensor
    - Allow 30 days for Licensor to respond and resolve the issue


13.4 EXTERNAL JURISDICTION:

If you are accessing the Game from outside India, you acknowledge that:
    - Local laws may also apply to you
    - You are responsible for compliance with local laws
    - Disputes may be subject to local courts if required by applicable law


14. SEVERABILITY

If any provision of this EULA is found to be invalid, illegal, or unenforceable 
by a court of competent jurisdiction:
    - That provision will be modified to the minimum extent necessary to make it 
      enforceable, or
    - That provision will be severed from this EULA
    - The remaining provisions will continue in full force and effect


15. ENTIRE AGREEMENT

15.1 This EULA, together with the Terms of Service and Privacy Policy, 
constitutes the entire agreement between you and Licensor regarding the Game.

15.2 This EULA supersedes any prior agreements, understandings, negotiations, or 
discussions between you and Licensor.

15.3 No modification of this EULA is valid unless in writing and signed by an 
authorized representative of Licensor.


16. AMENDMENTS

16.1 Licensor may modify this EULA at any time by posting the updated version 
on the Game or on our website.

16.2 Your continued use of the Game following publication of modified terms 
constitutes your acceptance of the changes.

16.3 We will make reasonable efforts to notify you of material changes.


17. CONTACT AND SUPPORT

For questions or concerns regarding this EULA:

[Company will provide contact details]

All inquiries will be responded to within 15 business days.


================================================================================
                                FINAL NOTES
================================================================================

IMPORTANT: These documents form a binding legal contract. By downloading and 
playing U10, you agree to all terms, conditions, and policies stated herein.

If you do not agree to any part of these documents, you are prohibited from 
using the Game.

For any questions, disputes, or concerns, contact GOB Interactive using the 
support information provided.

GOVERNING JURISDICTION: Republic of India
`;


// ─────────────────────────────────────────────────────────────────────────────
// AUTH DOMAIN
// ─────────────────────────────────────────────────────────────────────────────
"use strict";
// =============================================================================
// U10 — Auth Lifecycle & User Provisioning Module
// =============================================================================
// Responsibility: Owns everything related to WHO a user is.
//   - Hooks into post-authentication events (device + Google)
//   - Generates a unique 8-digit numeric username for new accounts
//   - Initializes the player profile storage record
//
// This module intentionally contains ZERO coin / economy logic.
// Coin grants are the responsibility of economy.ts (claim_signup_reward RPC).
// =============================================================================
// ---------------------------------------------------------------------------
// Constants
// ---------------------------------------------------------------------------
/** Inclusive lower bound for the 8-digit numeric username range. */
const USERNAME_MIN = 10000000;
/** Inclusive upper bound for the 8-digit numeric username range. */
const USERNAME_MAX = 99999999;
/** Maximum collision-retry attempts before giving up on username assignment. */
const USERNAME_RETRY_LIMIT = 10;
/**
 * Default player profile written to Nakama Storage on first registration.
 *
 * NOTE: coins intentionally starts at 0.
 * The first-time 1000c bonus is credited by the Economy module's
 * `claim_signup_reward` RPC when the client calls it after login.
 * This keeps the coin ledger entirely inside the Economy domain.
 */
const DEFAULT_PLAYER_STATS = {
    wins: 0,
    total_played: 0,
    best_streak: 0,
    lp: 0,
    tier: "Bronze",
    coins: 0,
    level: 1,
    xp: 0,
    login_streak: 0,
    last_login_claim: 0,
    current_cycle: 1,
    active_streak_shields: 0,
    welcome_back_eligible: false,
};
const DEFAULT_PLAYER_INVENTORY = {
    spin_tokens: 0,
    shields: { starter: 0, bronze: 0, silver: 0, gold: 0, platinum: 0 },
    unlocked_cosmetics: ["card_back_default"],
    equipped_cosmetics: { card_back: "card_back_default", avatar_ring: "" }
};
// ---------------------------------------------------------------------------
// Private helpers
// ---------------------------------------------------------------------------
/**
 * Attempts to assign a unique 8-digit numeric username to a newly created
 * user. Retries up to USERNAME_RETRY_LIMIT times on collision.
 *
 * @throws {Error} If no unique username can be found within the retry limit.
 */
function generateUniqueUsername(nk, logger, userId) {
    for (let attempt = 0; attempt < USERNAME_RETRY_LIMIT; attempt++) {
        const candidate = String(Math.floor(USERNAME_MIN + Math.random() * (USERNAME_MAX - USERNAME_MIN + 1)));
        try {
            nk.accountUpdateId(userId, candidate, null, null, null, null, null, null);
            logger.info(`[Auth] Assigned username ${candidate} to user ${userId}.`);
            return candidate;
        }
        catch (_e) {
            logger.warn(`[Auth] Username ${candidate} collision — retrying (${attempt + 1}/${USERNAME_RETRY_LIMIT}).`);
        }
    }
    throw new Error(`[Auth] Failed to assign unique username to ${userId} after ${USERNAME_RETRY_LIMIT} attempts.`);
}
/**
 * Writes the default player profile record to Nakama Storage.
 * Called once per user at the moment of first registration.
 *
 * Permission model:
 *   permissionRead  = 1 → Public read  (friends, leaderboards can see stats)
 *   permissionWrite = 1 → Owner write  (client can update non-critical fields)
 */
function initializePlayerProfile(nk, logger, userId) {
    nk.storageWrite([
        {
            collection: "player_stats",
            key: "stats",
            userId,
            value: DEFAULT_PLAYER_STATS,
            permissionRead: 1,
            permissionWrite: 1,
        },
        {
            collection: "player_inventory",
            key: "inventory",
            userId,
            value: DEFAULT_PLAYER_INVENTORY,
            permissionRead: 1,
            permissionWrite: 0, // Server-only write (tamper-proof)
        }
    ]);
    logger.info(`[Auth] Player profile and inventory initialized for user ${userId}.`);
}
// ---------------------------------------------------------------------------
// After-authenticate hook (runs only on brand-new account creation)
// ---------------------------------------------------------------------------
/**
 * Fires after a successful authentication for BOTH device and Google auth.
 * Only executes provisioning logic when `out.created === true` (new accounts).
 * Existing accounts silently pass through without any action.
 */
function onAfterAuthenticate(ctx, logger, nk, out, _request) {
    // Guard: only provision brand-new registrations
    if (!out.created)
        return;
    const userId = ctx.userId;
    logger.info(`[Auth] New user registered: ${userId}. Starting provisioning...`);
    // 1. Assign a unique 8-digit username
    try {
        generateUniqueUsername(nk, logger, userId);
    }
    catch (e) {
        // Non-fatal: user keeps the Nakama auto-generated username as fallback.
        // Account is still fully functional.
        logger.error(`[Auth] Username assignment error for ${userId}: ${e.message}`);
    }
    // 2. Create the player profile storage record
    try {
        initializePlayerProfile(nk, logger, userId);
    }
    catch (e) {
        // Non-fatal: EconomyManager on the client will handle missing records gracefully.
        logger.error(`[Auth] Profile initialization error for ${userId}: ${e.message}`);
    }
    logger.info(`[Auth] Provisioning complete for user ${userId}.`);
}
function updateProfileRpc(ctx, logger, nk, payload) {
    const userId = ctx.userId;
    if (!userId) {
        throw new Error("User ID not found in context");
    }
    let request;
    try {
        request = JSON.parse(payload);
    }
    catch (e) {
        throw new Error("Invalid request payload");
    }
    const displayName = request.display_name !== undefined ? request.display_name : null;
    const avatarUrl = request.avatar_url !== undefined ? request.avatar_url : null;
    const username = request.username !== undefined ? request.username : null;
    try {
        nk.accountUpdateId(userId, username, displayName, null, null, null, null, avatarUrl);
        logger.info(`[Auth] Profile updated for user ${userId}. DisplayName: ${displayName}, AvatarUrl: ${avatarUrl}, Username: ${username}`);
    }
    catch (e) {
        logger.error(`[Auth] Failed to update profile for user ${userId}: ${e.message}`);
        throw e;
    }
    return JSON.stringify({ success: true });
}
function beforeAuthenticateEmail(ctx, logger, nk, data) {
    var _a, _b;
    const email = (_a = data.account) === null || _a === void 0 ? void 0 : _a.email;
    if (email && email.endsWith("@u10game.internal")) {
        const consoleUsername = ctx.env["CONSOLE_USERNAME"] || "admin";
        const consolePassword = ctx.env["CONSOLE_PASSWORD"] || "defaultpassword";
        const username = email.split("@")[0];
        const password = (_b = data.account) === null || _b === void 0 ? void 0 : _b.password;
        if (username !== consoleUsername || password !== consolePassword) {
            logger.warn(`[Auth] Config Portal access denied for email: ${email}`);
            throw new Error("Invalid username or password for Config Portal.");
        }
        logger.info(`[Auth] Config Portal access granted for user: ${username}`);
        data.create = true;
        data.username = username;
    }
    return data;
}
// ---------------------------------------------------------------------------
// Module entry point — registers only auth-domain hooks
// ---------------------------------------------------------------------------


// ─────────────────────────────────────────────────────────────────────────────
// ECONOMY DOMAIN
// ─────────────────────────────────────────────────────────────────────────────
"use strict";
// =============================================================================
// U10 — Economy & Rewards Module (TypeScript)
// =============================================================================
// Responsibility: Owns everything related to WHAT a player earns and spends.
//   - Daily login streak cycle scaling & item rewards
//   - Streak recovery & streak shields
//   - Spin wheel segments, soft caps & jackpots
//   - Optional ad multipliers (max 3/day)
//   - Server-authoritative match entry validation & loss protection
//   - XP progression & level milestone rewards
// =============================================================================
// ---------------------------------------------------------------------------
// Constants
// ---------------------------------------------------------------------------
/** Base coin rewards for Day 1–7 (Cycle 1). */
const DAILY_REWARDS_BASE = [50, 75, 100, 125, 150, 200, 350];

// ---------------------------------------------------------------------------
// Storage helpers
// ---------------------------------------------------------------------------
function readPlayerStats(nk, userId) {
    const defaults = {
        wins: 0, total_played: 0, best_streak: 0, lp: 0,
        tier: "Bronze", coins: 0, level: 1, xp: 0,
        last_wheel_spin: 0,
        current_cycle: 1, active_streak_shields: 0, welcome_back_eligible: false,
        weekly_claims: [false, false, false, false, false, false, false],
        week_number: 0,
        week_year: 0,
        // Seasonal bonus tracking
        last_first_match_date: 0, // UTC timestamp (ms) of last first-match-of-day bonus
        current_win_streak: 0, // Live win streak — resets on any loss
    };
    const result = nk.storageRead([{ collection: "player_stats", key: "stats", userId }]);
    return (result && result.length > 0) ? Object.assign(Object.assign({}, defaults), result[0].value) : defaults;
}
function writePlayerStats(nk, userId, stats) {
    nk.storageWrite([{
            collection: "player_stats",
            key: "stats",
            userId,
            value: stats,
            permissionRead: 1,
            permissionWrite: 1,
        }]);
}
function readPlayerInventory(nk, userId) {
    const defaults = {
        spin_tokens: 0,
        shields: { starter: 0, bronze: 0, silver: 0, gold: 0, platinum: 0 },
        unlocked_cosmetics: ["card_back_default"],
        equipped_cosmetics: { card_back: "card_back_default", avatar_ring: "" }
    };
    const result = nk.storageRead([{ collection: "player_inventory", key: "inventory", userId }]);
    return (result && result.length > 0) ? Object.assign(Object.assign({}, defaults), result[0].value) : defaults;
}
function writePlayerInventory(nk, userId, inventory) {
    nk.storageWrite([{
            collection: "player_inventory",
            key: "inventory",
            userId,
            value: inventory,
            permissionRead: 1,
            permissionWrite: 0, // Server-only write (tamper-proof)
        }]);
}
function readPlayerDailyLimits(nk, userId) {
    const defaults = {
        spins_today_count: 0,
        spin_coins_today: 0,
        ad_multipliers_today: 0,
        last_spin_timestamp: 0,
        last_jackpot_timestamp: 0,
        last_ad_multiplier_timestamp: 0,
        paid_spins_today: 0,
        ad_spins_today: 0
    };
    const result = nk.storageRead([{ collection: "player_daily_limits", key: "limits", userId }]);
    if (result && result.length > 0) {
        const data = result[0].value;
        const now = new Date();
        const lastDate = new Date(data.last_spin_timestamp || 0);
        const isNewDay = now.getUTCDate() !== lastDate.getUTCDate() ||
            now.getUTCMonth() !== lastDate.getUTCMonth() ||
            now.getUTCFullYear() !== lastDate.getUTCFullYear();
        if (isNewDay) {
            data.spins_today_count = 0;
            data.spin_coins_today = 0;
            data.ad_multipliers_today = 0;
            data.paid_spins_today = 0;
            data.ad_spins_today = 0;
        }
        return Object.assign(Object.assign({}, defaults), data);
    }
    return defaults;
}
function writePlayerDailyLimits(nk, userId, limits) {
    nk.storageWrite([{
            collection: "player_daily_limits",
            key: "limits",
            userId,
            value: limits,
            permissionRead: 1,
            permissionWrite: 0, // Server-only write
        }]);
}
// ---------------------------------------------------------------------------
// Helpers for Calendar-Week Daily Rewards
// ---------------------------------------------------------------------------
function getISOWeek(date) {
    const d = new Date(Date.UTC(date.getFullYear(), date.getMonth(), date.getDate()));
    const dayNum = d.getUTCDay() || 7;
    d.setUTCDate(d.getUTCDate() + 4 - dayNum);
    const yearStart = new Date(Date.UTC(d.getUTCFullYear(), 0, 1));
    const week = Math.ceil((((d.getTime() - yearStart.getTime()) / 86400000) + 1) / 7);
    return { week, year: d.getUTCFullYear() };
}
function getTodayDayIndex() {
    const day = new Date().getUTCDay(); // 0=Sun, 1=Mon...6=Sat
    return day === 0 ? 6 : day - 1; // Convert to 0=Mon...6=Sun
}
function getDailyRewardsStatusRpc(ctx, logger, nk, _payload) {
    const userId = ctx.userId;
    if (!userId)
        throw new Error("Unauthenticated request.");
    const stats = readPlayerStats(nk, userId);
    const now = new Date();
    const { week, year } = getISOWeek(now);
    const todayIndex = getTodayDayIndex();
    // Week rollover check
    if (stats.week_number !== week || stats.week_year !== year) {
        const prevFullWeek = (stats.weekly_claims || []).every((c) => c === true);
        if (prevFullWeek) {
            stats.current_cycle = Math.min((stats.current_cycle || 1) + 1, 3);
        }
        else {
            stats.current_cycle = 1;
        }
        stats.weekly_claims = [false, false, false, false, false, false, false];
        stats.week_number = week;
        stats.week_year = year;
        writePlayerStats(nk, userId, stats);
        logger.info(`[Economy] New ISO week detected (${week}/${year}). Rollover triggered. Cycle: ${stats.current_cycle}`);
    }
    const isTodayClaimed = stats.weekly_claims[todayIndex];
    return JSON.stringify({
        success: true,
        weekly_claims: stats.weekly_claims,
        today_index: todayIndex,
        is_today_claimed: isTodayClaimed,
        current_cycle: stats.current_cycle,
    });
}
// ---------------------------------------------------------------------------
// RPC: claim_daily_login
// ---------------------------------------------------------------------------
function claimDailyLoginRpc(ctx, logger, nk, _payload) {
    const userId = ctx.userId;
    if (!userId)
        throw new Error("Unauthenticated request.");
    const stats = readPlayerStats(nk, userId);
    const inventory = readPlayerInventory(nk, userId);
    const now = new Date();
    const { week, year } = getISOWeek(now);
    const todayIndex = getTodayDayIndex();
    // Week rollover check
    if (stats.week_number !== week || stats.week_year !== year) {
        const prevFullWeek = (stats.weekly_claims || []).every((c) => c === true);
        if (prevFullWeek) {
            stats.current_cycle = Math.min((stats.current_cycle || 1) + 1, 3);
        }
        else {
            stats.current_cycle = 1;
        }
        stats.weekly_claims = [false, false, false, false, false, false, false];
        stats.week_number = week;
        stats.week_year = year;
    }
    // Check if today already claimed
    if (stats.weekly_claims[todayIndex]) {
        return JSON.stringify({ success: false, error: "Already claimed today." });
    }
    // Calculate reward
    const cycle = Math.min(stats.current_cycle || 1, 3);
    const baseCoins = DAILY_REWARDS_BASE[todayIndex];
    let coinsGranted = baseCoins;
    // Add Cycle Daily scaling (+5c per day for Cycle 2, +10c per day for Cycle 3+)
    coinsGranted += (cycle - 1) * 5;
    let allDaysClaimedBonusApplied = false;
    if (todayIndex === 6) { // Sunday
        // Check if Mon-Sat (all previous 6 days) were claimed
        const allPrevClaimed = stats.weekly_claims.slice(0, 6).every((c) => c === true);
        if (!allPrevClaimed) {
            return JSON.stringify({ success: false, error: "You missed a day this week, so you can't claim the Day 7 chest. Try again next week! Keep going, cheer up!" });
        }
        // Award Sunday Streak Bonus: 150c base + (cycle-1)*25
        coinsGranted += 150 + (cycle - 1) * 25;
        allDaysClaimedBonusApplied = true;
    }
    // Update claim flags
    stats.weekly_claims[todayIndex] = true;
    stats.coins += coinsGranted;
    // Wallet update
    nk.walletUpdate(userId, { coins: coinsGranted }, { source: "daily_login_calendar", day_index: todayIndex, cycle: cycle });
    // Award GDD items
    let grantedItemName = "";
    if (todayIndex === 2) { // Wednesday (Day 3)
        inventory.spin_tokens += 1;
        grantedItemName = "1x Extra Wheel Spin Token";
    }
    else if (todayIndex === 4) { // Friday (Day 5)
        inventory.shields.starter = Math.min((inventory.shields.starter || 0) + 1, 3);
        grantedItemName = "1x Starter Arena Shield";
    }
    else if (todayIndex === 6) { // Sunday (Day 7)
        inventory.spin_tokens += 1;
        stats.active_streak_shields = 1; // Award 1 Streak Shield (cap is 1)
        // Check cycle unlock for cosmetic card back (cap at cycle 3)
        const cosmeticId = `card_back_cycle_${cycle}`;
        if (inventory.unlocked_cosmetics.indexOf(cosmeticId) === -1) {
            inventory.unlocked_cosmetics.push(cosmeticId);
        }
        grantedItemName = `1x Spin Token + 1x Streak Shield + Card Back Cycle ${cycle}`;
    }
    writePlayerStats(nk, userId, stats);
    writePlayerInventory(nk, userId, inventory);
    logger.info(`[Economy] Player ${userId} claimed Day Index ${todayIndex} reward: +${coinsGranted}c. ${grantedItemName}`);
    return JSON.stringify({
        success: true,
        coins: stats.coins,
        reward_claimed: coinsGranted,
        item_claimed: grantedItemName,
        weekly_claims: stats.weekly_claims,
        current_cycle: stats.current_cycle,
        today_index: todayIndex,
        is_today_claimed: true,
        all_days_claimed_bonus_applied: allDaysClaimedBonusApplied
    });
}
// ---------------------------------------------------------------------------
// RPC: spin_wheel
// ---------------------------------------------------------------------------
function spinWheelRpc(ctx, logger, nk, payload) {
    const userId = ctx.userId;
    if (!userId)
        throw new Error("Unauthenticated request.");
    const stats = readPlayerStats(nk, userId);
    const inventory = readPlayerInventory(nk, userId);
    const limits = readPlayerDailyLimits(nk, userId);
    const parsed = payload ? JSON.parse(payload) : {};
    const isExtraSpin = parsed.is_extra_spin === true;
    const now = Date.now();
    const COOLDOWN_MS = 86400000;
    // 1. Cooldown or token verification / cost deduction
    const useAd = parsed.use_ad === true;
    let cost = 0;
    if (useAd) {
        limits.ad_spins_today = (limits.ad_spins_today || 0) + 1;
    }
    else if (isExtraSpin) {
        if (inventory.spin_tokens > 0) {
            inventory.spin_tokens -= 1;
        }
        else {
            // Paid extra spin
            const paidCount = limits.paid_spins_today || 0;
            if (paidCount === 0) {
                cost = 250;
            }
            else if (paidCount === 1) {
                cost = 350;
            }
            else if (paidCount === 2) {
                cost = 420;
            }
            else {
                cost = 500 + (paidCount - 3) * 100;
            }
            if (stats.coins < cost) {
                return JSON.stringify({ success: false, error: "Insufficient coins for extra spin." });
            }
            stats.coins -= cost;
            limits.paid_spins_today = paidCount + 1;
            nk.walletUpdate(userId, { coins: -cost }, { source: "spin_wheel_paid", cost: cost });
        }
    }
    else {
        if (stats.last_wheel_spin && (now - stats.last_wheel_spin) < COOLDOWN_MS) {
            return JSON.stringify({ success: false, error: "Free daily spin is on cooldown." });
        }
        stats.last_wheel_spin = now;
    }
    limits.spins_today_count += 1;
    limits.last_spin_timestamp = now;
    // 2. Select Rarity Tier (8 segments 1:1)
    const roll = Math.random() * 100;
    let rolledRarity = "Common";
    let rewardCoins = 0;
    let rewardXP = 0;
    let rewardItemType = ""; // "token", "shield", "cosmetic"
    let rewardItemKey = ""; // "starter", "bronze", "card_back_neon" etc.
    let displayMessage = "";
    let segmentIndex = 0;
    if (roll < 35) {
        // Segment 0: 50 Coins (Common, 35%)
        rolledRarity = "Common";
        segmentIndex = 0;
        rewardCoins = 50;
        displayMessage = "50 Coins";
    }
    else if (roll < 60) {
        // Segment 1: 150 Coins (Uncommon, 25%)
        rolledRarity = "Uncommon";
        segmentIndex = 1;
        rewardCoins = 150;
        displayMessage = "150 Coins";
    }
    else if (roll < 75) {
        // Segment 2: 300 Coins (Rare, 15%)
        rolledRarity = "Rare";
        segmentIndex = 2;
        rewardCoins = 300;
        displayMessage = "300 Coins";
    }
    else if (roll < 85) {
        // Segment 3: 1x Extra Spin Token (10%)
        rolledRarity = "Uncommon";
        segmentIndex = 3;
        rewardItemType = "token";
        rewardItemKey = "spin";
        displayMessage = "1x Extra Spin Token";
    }
    else if (roll < 93) {
        // Segment 4: 1x Bronze Shield (8%)
        rolledRarity = "Rare";
        segmentIndex = 4;
        rewardItemType = "shield";
        rewardItemKey = "bronze";
        displayMessage = "1x Bronze Shield";
    }
    else if (roll < 97) {
        // Segment 5: 1x Silver Shield (4%)
        rolledRarity = "Epic";
        segmentIndex = 5;
        rewardItemType = "shield";
        rewardItemKey = "silver";
        displayMessage = "1x Silver Shield";
    }
    else if (roll < 99) {
        // Segment 6: Neon Skin (2%)
        rolledRarity = "Legendary";
        segmentIndex = 6;
        rewardItemType = "cosmetic";
        rewardItemKey = "card_back_neon";
        displayMessage = "Neon Card Back Skin";
    }
    else {
        // Segment 7: Jackpot 1000 Coins (1%)
        rolledRarity = "Legendary";
        segmentIndex = 7;
        rewardCoins = 1000;
        displayMessage = "1,000 Coins Jackpot!";
    }
    // 3. Enforce 800c Daily Spin Soft Cap
    if (rewardCoins > 0 && (limits.spin_coins_today + rewardCoins) > 800) {
        logger.info(`[Economy] Player ${userId} hit daily spin coin soft cap of 800c. Converting coin reward to XP.`);
        rewardCoins = 0;
        rewardXP = 50;
        displayMessage = "50 XP (Soft Cap Conversion)";
    }
    // 4. Enforce Weekly Jackpot Cooldown
    if (rewardCoins >= 1000) {
        const oneWeekMs = 7 * 24 * 60 * 60 * 1000;
        if (limits.last_jackpot_timestamp && (now - limits.last_jackpot_timestamp) < oneWeekMs) {
            logger.info(`[Economy] Player ${userId} hit jackpot cooldown. Downgrading jackpot.`);
            rewardCoins = 300;
            displayMessage = "300 Coins (Jackpot Cooldown applied)";
        }
        else {
            limits.last_jackpot_timestamp = now;
        }
    }
    // 5. Apply Rewards
    if (rewardCoins > 0) {
        stats.coins += rewardCoins;
        limits.spin_coins_today += rewardCoins;
        nk.walletUpdate(userId, { coins: rewardCoins }, { source: "spin_wheel", rarity: rolledRarity });
    }
    if (rewardXP > 0) {
        stats.xp += rewardXP;
        evaluateLevelUp(stats, inventory, logger);
    }
    if (rewardItemType === "token") {
        inventory.spin_tokens += 1;
    }
    else if (rewardItemType === "shield") {
        inventory.shields[rewardItemKey] = Math.min((inventory.shields[rewardItemKey] || 0) + 1, 3);
    }
    else if (rewardItemType === "cosmetic") {
        if (inventory.unlocked_cosmetics.indexOf(rewardItemKey) === -1) {
            inventory.unlocked_cosmetics.push(rewardItemKey);
        }
    }
    writePlayerStats(nk, userId, stats);
    writePlayerInventory(nk, userId, inventory);
    writePlayerDailyLimits(nk, userId, limits);
    logger.info(`[Economy] Player ${userId} spun wheel: Segment=${segmentIndex}, Rarity=${rolledRarity}, Result=${displayMessage}`);
    return JSON.stringify({
        success: true,
        rarity: rolledRarity,
        segment_index: segmentIndex,
        coins_won: rewardCoins,
        xp_won: rewardXP,
        item_type: rewardItemType,
        item_key: rewardItemKey,
        display_message: displayMessage,
        total_coins: stats.coins,
        spin_tokens: inventory.spin_tokens,
        spins_today: limits.spins_today_count,
        paid_spins_today: limits.paid_spins_today,
        ad_spins_today: limits.ad_spins_today
    });
}
// ---------------------------------------------------------------------------
// RPC: apply_ad_multiplier
// ---------------------------------------------------------------------------
function applyAdMultiplierRpc(ctx, logger, nk, payload) {
    const userId = ctx.userId;
    if (!userId)
        throw new Error("Unauthenticated request.");
    const parsed = JSON.parse(payload);
    const rewardType = parsed.reward_type; // "coins" or "tokens"
    const amount = parsed.amount || 0;
    if (amount <= 0 || (rewardType !== "coins" && rewardType !== "tokens")) {
        return JSON.stringify({ success: false, error: "Invalid reward type or amount." });
    }
    const limits = readPlayerDailyLimits(nk, userId);
    // Enforce 3 rewarded ads daily cap
    if (limits.ad_multipliers_today >= 3) {
        return JSON.stringify({ success: false, error: "Daily ad multiplier limit reached." });
    }
    limits.ad_multipliers_today += 1;
    limits.last_ad_multiplier_timestamp = Date.now();
    const stats = readPlayerStats(nk, userId);
    const inventory = readPlayerInventory(nk, userId);
    if (rewardType === "coins") {
        // 2x standard multiplier means we award the base amount a second time
        stats.coins += amount;
        nk.walletUpdate(userId, { coins: amount }, { source: "ad_multiplier" });
    }
    else if (rewardType === "tokens") {
        inventory.spin_tokens += amount;
    }
    writePlayerStats(nk, userId, stats);
    writePlayerInventory(nk, userId, inventory);
    writePlayerDailyLimits(nk, userId, limits);
    logger.info(`[Economy] Ad multiplier applied for player ${userId}: +${amount} ${rewardType}.`);
    return JSON.stringify({
        success: true,
        ad_count: limits.ad_multipliers_today,
        total_coins: stats.coins,
        spin_tokens: inventory.spin_tokens
    });
}
// ---------------------------------------------------------------------------
// RPC: get_arena_config
// ---------------------------------------------------------------------------
function readArenaConfig(nk) {
    var result = nk.storageRead([{
            collection: "system_config",
            key: "arena_list",
            userId: "00000000-0000-0000-0000-000000000000",
        }]);
    if (result && result.length > 0 && result[0].value) {
        return result[0].value;
    }
    
    // Seed the DB on first load using injected defaults
    var defaults = JSON.parse('{"practice":{"name":"WARM-UP","badge":"BEGINNER","badge_color":"red","entry_fee":0,"offer_fee":0,"desc":"Play for free with bots. Gain experience.","gradient":"red_magenta","order":0,"jackpot":0,"tier_label":"","shield_image":"","height_variant":"standard","glow_enabled":false,"glow_color":""},"starter":{"name":"WOODEN LEAGUE","badge":"LEAGUE I","badge_color":"teal","entry_fee":50,"offer_fee":0,"desc":"Beginner-friendly arena.","gradient":"teal_cyan","order":1,"jackpot":0,"tier_label":"","shield_image":"","height_variant":"standard","glow_enabled":false,"glow_color":""},"bronze":{"name":"IRON FORGE","badge":"LEAGUE II","badge_color":"pink","entry_fee":250,"offer_fee":0,"desc":"Step up the competition.","gradient":"pink_magenta","order":2,"jackpot":0,"tier_label":"","shield_image":"","height_variant":"standard","glow_enabled":false,"glow_color":""},"silver":{"name":"SILVER LEAGUE","badge":"LEAGUE III","badge_color":"silver","entry_fee":500,"offer_fee":0,"desc":"Balanced play for skilled callers.","gradient":"silver_blue","order":3,"jackpot":0,"tier_label":"","shield_image":"","height_variant":"standard","glow_enabled":false,"glow_color":""},"gold":{"name":"DIAMOND LOUNGE","badge":"ELITE ONLY","badge_color":"purple","entry_fee":1000,"offer_fee":0,"desc":"High stakes, maximum rewards. Pure chaos awaits.","gradient":"purple_deep","order":4,"jackpot":50000,"tier_label":"JACKPOT","shield_image":"","height_variant":"tall","glow_enabled":true,"glow_color":"#ff3b80"},"platinum":{"name":"DIAMOND LEAGUE","badge":"LEGENDARY ONLY","badge_color":"gold","entry_fee":2000,"offer_fee":0,"desc":"The pinnacle of skill. Only for the true masters of chaos.","gradient":"navy_gold","order":5,"jackpot":0,"tier_label":"ENTRY FEE","shield_image":"","height_variant":"standard","glow_enabled":true,"glow_color":"#ffd700"}}');
    nk.storageWrite([{
            collection: "system_config",
            key: "arena_list",
            userId: "00000000-0000-0000-0000-000000000000",
            value: defaults,
            permissionRead: 2,
            permissionWrite: 0,
        }]);
    return defaults;
}
function readArenaOffers(nk) {
    var result = nk.storageRead([{
        collection: "system_config",
        key: "arena_offers",
        userId: "00000000-0000-0000-0000-000000000000",
    }]);
    if (result && result.length > 0 && result[0].value) {
        return result[0].value;
    }
    return {};
}
function getArenaConfigRpc(ctx, logger, nk, payload) {
    var baseConfig = readArenaConfig(nk);
    var offers = readArenaOffers(nk);
    var arenas = [];
    for (var tier in baseConfig) {
        var config = baseConfig[tier];
        var arena = Object.assign({}, config, { id: tier });
        if (offers[tier] !== undefined && offers[tier] !== null) {
            var offerFee = Number(offers[tier]);
            if (offerFee >= 0 && offerFee < arena.entry_fee) {
                arena.offer_fee = offerFee;
            }
        }
        arenas.push(arena);
    }
    arenas.sort(function(a, b) { return a.order - b.order; });
    logger.info("[Economy] Arena config served. " + arenas.length + " arenas, " + Object.keys(offers).length + " active offers.");
    return JSON.stringify({ success: true, arenas: arenas });
}
// ---------------------------------------------------------------------------
// RPC: start_match
// ---------------------------------------------------------------------------
function startMatchRpc(ctx, logger, nk, payload) {
    const userId = ctx.userId;
    if (!userId)
        throw new Error("Unauthenticated request.");
    const parsed = JSON.parse(payload);
    const arenaTier = parsed.arena_tier;
    const isPrivate = parsed.is_private === true;
    
    var baseConfig = readArenaConfig(nk);
    var arena = baseConfig[arenaTier];
    if (!arena) {
        return JSON.stringify({ success: false, error: "Invalid arena tier." });
    }
    let entryFee = arena.entry_fee;
    
    // Apply active offer pricing if available
    var offers = readArenaOffers(nk);
    if (offers[arenaTier] !== undefined && offers[arenaTier] !== null) {
        var offerFee = Number(offers[arenaTier]);
        if (offerFee >= 0 && offerFee < entryFee) {
            entryFee = offerFee;
        }
    }
    const stats = readPlayerStats(nk, userId);
    // If match is private and host level >= 500, match is free to host/join
    const effectiveEntryFee = (isPrivate && stats.level >= 500) ? 0 : entryFee;
    if (stats.coins < effectiveEntryFee) {
        return JSON.stringify({ success: false, error: "Insufficient coins to enter match." });
    }
    // 1. Resolve any stale active match
    const activeRead = nk.storageRead([{ collection: "player_active_match", key: "active", userId }]);
    if (activeRead && activeRead.length > 0) {
        const active = activeRead[0].value;
        const elapsed = Date.now() - active.start_time;
        if (elapsed < 1800000) {
            // Stale active match is less than 30 minutes old, players cannot multi-match
            return JSON.stringify({ success: false, error: "An active match is already in progress.", match_id: active.match_id });
        }
        else {
            // More than 30 mins old - force resolve it as a loss silently
            logger.warn(`[Economy] Auto-resolving stale match ${active.match_id} as loss.`);
            forceResolveMatchLoss(nk, userId, active.arena_tier, active.entry_fee, logger);
        }
    }
    // 2. Deduct entry fee if > 0
    if (effectiveEntryFee > 0) {
        stats.coins -= effectiveEntryFee;
        writePlayerStats(nk, userId, stats);
        nk.walletUpdate(userId, { coins: -effectiveEntryFee }, { source: "match_entry", tier: arenaTier });
    }
    // 3. Write active match tracking state
    const matchId = nk.uuidV4();
    nk.storageWrite([{
            collection: "player_active_match",
            key: "active",
            userId,
            value: { match_id: matchId, arena_tier: arenaTier, entry_fee: effectiveEntryFee, start_time: Date.now(), is_private: isPrivate },
            permissionRead: 1,
            permissionWrite: 0 // Server-only write
        }]);
    logger.info(`[Economy] Match started for player ${userId}. Match ID: ${matchId}, Tier: ${arenaTier}, Fee: ${effectiveEntryFee}, Private: ${isPrivate}`);
    return JSON.stringify({ success: true, match_id: matchId });
}
// ---------------------------------------------------------------------------
// RPC: end_match
// ---------------------------------------------------------------------------
function endMatchRpc(ctx, logger, nk, payload) {
    const userId = ctx.userId;
    if (!userId)
        throw new Error("Unauthenticated request.");
    const parsed = JSON.parse(payload);
    const matchId = parsed.match_id;
    const won = parsed.won === true;
    const activeRead = nk.storageRead([{ collection: "player_active_match", key: "active", userId }]);
    if (!activeRead || activeRead.length === 0) {
        return JSON.stringify({ success: false, error: "No active match found for this player." });
    }
    const activeMatch = activeRead[0].value;
    if (activeMatch.match_id !== matchId) {
        return JSON.stringify({ success: false, error: "Invalid active match verification." });
    }
    const arenaTier = activeMatch.arena_tier;
    const entryFee = activeMatch.entry_fee;
    const stats = readPlayerStats(nk, userId);
    const inventory = readPlayerInventory(nk, userId);
    const isPrivate = activeMatch.is_private === true;
    let shieldConsumed = false;
    let coinsRefunded = 0;
    let xpGained = 0;
    stats.total_played += 1;
    let payout = 0;
    if (won) {
        // Winner payout (2x entry fee)
        payout = entryFee * 2;
        stats.coins += payout;
        stats.wins += 1;
        xpGained = 80; // GDD: +80 XP for winning
        if (payout > 0) {
            nk.walletUpdate(userId, { coins: payout }, { source: "match_win", match_id: matchId });
        }
    }
    else {
        // Loss - Check if player has a tier shield (only if not practice/private with 0 entry fee)
        const shieldCount = entryFee > 0 ? (inventory.shields[arenaTier] || 0) : 0;
        if (shieldCount > 0) {
            inventory.shields[arenaTier] -= 1;
            shieldConsumed = true;
            coinsRefunded = entryFee;
            stats.coins += entryFee; // Refund entry fee
            xpGained = 25; // GDD: +25 XP even for loss
            nk.walletUpdate(userId, { coins: entryFee }, { source: "match_loss_shielded", match_id: matchId });
        }
        else {
            xpGained = 25; // Normal loss
        }
    }
    stats.xp += xpGained;
    const oldLevel = stats.level;
    evaluateLevelUp(stats, inventory, logger);
    // Write updated states
    writePlayerStats(nk, userId, stats);
    writePlayerInventory(nk, userId, inventory);
    // Delete active match token
    nk.storageDelete([{ collection: "player_active_match", key: "active", userId }]);
    // --- Seasonal XP Grant ---
    const sxpTable = SXP_BY_MODE[arenaTier] || SXP_BY_MODE['bronze'];
    let sxpGained = won ? sxpTable.win : sxpTable.loss;
    // ── First Match of Day Bonus (+50 SXP, non-practice only) ──────────────────
    let firstMatchBonus = 0;
    if (arenaTier !== 'practice' && !isPrivate) {
        const nowMs = Date.now();
        const todayUTCMidnight = new Date();
        todayUTCMidnight.setUTCHours(0, 0, 0, 0);
        const lastDate = new Date(stats.last_first_match_date || 0);
        lastDate.setUTCHours(0, 0, 0, 0);
        if (lastDate.getTime() < todayUTCMidnight.getTime()) {
            firstMatchBonus = 50;
            stats.last_first_match_date = nowMs;
            logger.info(`[Economy] First match of day bonus: +50 SXP for player ${userId}`);
        }
    }
    // ── Win-Streak Bonus (+40 SXP every 3rd consecutive win) ───────────────────
    let streakBonus = 0;
    if (won) {
        stats.current_win_streak = (stats.current_win_streak || 0) + 1;
        if (stats.current_win_streak > (stats.best_streak || 0)) {
            stats.best_streak = stats.current_win_streak;
        }
        if (stats.current_win_streak % 3 === 0) {
            streakBonus = 40;
            logger.info(`[Economy] Win streak bonus: +40 SXP for player ${userId} (streak: ${stats.current_win_streak})`);
        }
    }
    else {
        stats.current_win_streak = 0;
    }
    sxpGained += firstMatchBonus + streakBonus;
    grantSeasonalXP(nk, logger, userId, sxpGained, `match_${won ? 'win' : 'loss'}_${arenaTier}`);
    // Rank Progression LP calculation (ranked matches only - non-practice and non-private)
    let lpDelta = 0;
    if (!isPrivate && arenaTier !== 'practice') {
        const currentTier = stats.tier || "Bronze";
        if (won) {
            if (currentTier === "Bronze" || currentTier === "Silver") {
                lpDelta = 25;
            }
            else if (currentTier === "Gold" || currentTier === "Platinum") {
                lpDelta = 20;
            }
            else if (currentTier === "Elite" || currentTier === "Master") {
                lpDelta = 15;
            }
            else { // Prestige
                lpDelta = 10;
            }
            stats.lp = (stats.lp || 0) + lpDelta;
        }
        else {
            if (currentTier === "Bronze" || currentTier === "Silver") {
                lpDelta = -10;
            }
            else if (currentTier === "Gold" || currentTier === "Platinum") {
                lpDelta = -15;
            }
            else if (currentTier === "Elite" || currentTier === "Master") {
                lpDelta = -18;
            }
            else { // Prestige
                lpDelta = -20;
            }
            stats.lp = Math.max(0, (stats.lp || 0) + lpDelta);
        }
        stats.tier = calculateTier(stats.lp);
    }
    // Update weekly challenge counters
    const weeklyUpdates = {
        matches_this_week: 1,
        match_coins_this_week: won ? (entryFee * 2) : 0,
        best_streak_this_week: stats.current_win_streak,
    };
    if (won) {
        weeklyUpdates.wins_this_week = 1;
        if (arenaTier === 'gold' || arenaTier === 'diamond' || arenaTier === 'sapphire') {
            weeklyUpdates.gold_plus_wins_this_week = 1;
        }
    }
    if (arenaTier === 'sapphire') {
        weeklyUpdates.sapphire_matches_this_week = 1;
    }
    updateWeeklyProgress(nk, logger, userId, weeklyUpdates);
    // Write Match History Record
    const coinsGained = won ? payout : (shieldConsumed ? entryFee : 0);
    writeMatchHistoryRecord(nk, userId, matchId, arenaTier, won, lpDelta, coinsGained, isPrivate);
    // Evaluate & Update achievements progression
    updateAchievementsProgress(nk, logger, userId, stats);
    // Final stats write
    writePlayerStats(nk, userId, stats);
    logger.info(`[Economy] Match resolved: MatchId=${matchId}, Player=${userId}, Won=${won}, LP Delta=${lpDelta}, Tier=${stats.tier}, ShieldConsumed=${shieldConsumed}, XP Gained=${xpGained}, SXP Gained=${sxpGained}`);
    return JSON.stringify({
        success: true,
        won,
        xp_gained: xpGained,
        sxp_gained: sxpGained,
        first_match_bonus: firstMatchBonus,
        streak_bonus: streakBonus,
        shield_consumed: shieldConsumed,
        refunded_coins: coinsRefunded,
        new_coins: stats.coins,
        new_level: stats.level,
        level_up: stats.level > oldLevel,
        lp_delta: lpDelta,
        new_lp: stats.lp,
        new_tier: stats.tier
    });
}
// ---------------------------------------------------------------------------
// RPC: get_active_match (for reconnection)
// ---------------------------------------------------------------------------
function getActiveMatchRpc(ctx, logger, nk, _payload) {
    const userId = ctx.userId;
    if (!userId)
        throw new Error("Unauthenticated request.");
    const activeRead = nk.storageRead([{ collection: "player_active_match", key: "active", userId }]);
    if (activeRead && activeRead.length > 0) {
        const active = activeRead[0].value;
        const elapsed = Date.now() - active.start_time;
        if (elapsed < 1800000) {
            // Active and not timed out
            return JSON.stringify({ active: true, match_id: active.match_id, arena_tier: active.arena_tier, entry_fee: active.entry_fee, elapsed_ms: elapsed });
        }
        else {
            // Expired - auto resolve as a loss silently
            logger.warn(`[Economy] Auto-resolving expired active match on query: ${active.match_id}`);
            forceResolveMatchLoss(nk, userId, active.arena_tier, active.entry_fee, logger);
            nk.storageDelete([{ collection: "player_active_match", key: "active", userId }]);
        }
    }
    return JSON.stringify({ active: false });
}
// ---------------------------------------------------------------------------
// Private Helpers / Game Mechanics
// ---------------------------------------------------------------------------
function forceResolveMatchLoss(nk, userId, arenaTier, entryFee, logger) {
    const stats = readPlayerStats(nk, userId);
    const inventory = readPlayerInventory(nk, userId);
    stats.total_played += 1;
    stats.xp += 25; // Award loss XP
    // Check if they have a shield
    const shieldCount = inventory.shields[arenaTier] || 0;
    if (shieldCount > 0) {
        inventory.shields[arenaTier] -= 1;
        stats.coins += entryFee; // Refund entry fee
        nk.walletUpdate(userId, { coins: entryFee }, { source: "match_loss_shielded_timeout" });
    }
    evaluateLevelUp(stats, inventory, logger);
    writePlayerStats(nk, userId, stats);
    writePlayerInventory(nk, userId, inventory);
}
function evaluateLevelUp(stats, inventory, logger) {
    // Cap at 500
    if (stats.level >= 500) {
        stats.level = 500;
        stats.xp = 0;
        stats.is_max_level_vip = true;
        return;
    }
    // Level threshold calculation: easy up to 75, then scaling up to level 500
    const getXpThreshold = (level) => {
        if (level <= 75)
            return 100;
        if (level <= 150)
            return 500;
        if (level <= 300)
            return 1000;
        return 2000;
    };
    let currentThreshold = getXpThreshold(stats.level);
    while (stats.xp >= currentThreshold) {
        if (stats.level >= 500) {
            stats.level = 500;
            stats.xp = 0;
            stats.is_max_level_vip = true;
            break;
        }
        stats.xp -= currentThreshold;
        stats.level += 1;
        currentThreshold = getXpThreshold(stats.level);
        if (stats.level === 500) {
            stats.is_max_level_vip = true;
            logger.info(`[LevelUp] Player reached max level 500! VIP perks enabled.`);
        }
        // Milestone Level Rewards (Coins / Tokens / Cosmetics)
        let rewardCoins = 0;
        let unlockedCosmetic = "";
        if (stats.level === 3) {
            rewardCoins = 100;
        }
        else if (stats.level === 5) {
            rewardCoins = 200;
        }
        else if (stats.level === 10) {
            rewardCoins = 500;
            unlockedCosmetic = "card_back_bronze";
        }
        else if (stats.level === 15) {
            rewardCoins = 750;
            unlockedCosmetic = "avatar_ring_bronze";
        }
        else if (stats.level === 20) {
            rewardCoins = 1000;
            unlockedCosmetic = "card_back_silver";
        }
        else if (stats.level === 25) {
            rewardCoins = 1000;
            unlockedCosmetic = "avatar_ring_silver";
        }
        else if (stats.level === 30) {
            rewardCoins = 1500;
            unlockedCosmetic = "card_back_gold";
        }
        else if (stats.level === 35) {
            rewardCoins = 2000;
            unlockedCosmetic = "card_back_animated";
        }
        else if (stats.level === 40) {
            rewardCoins = 2500;
            unlockedCosmetic = "avatar_ring_gold";
        }
        else if (stats.level === 45) {
            rewardCoins = 3000;
            unlockedCosmetic = "avatar_frame_rare_animated";
        }
        else if (stats.level === 50) {
            rewardCoins = 5000;
            unlockedCosmetic = "card_back_diamond";
        }
        // Grant level up perks
        if (rewardCoins > 0) {
            stats.coins += rewardCoins;
        }
        if (unlockedCosmetic) {
            if (inventory.unlocked_cosmetics.indexOf(unlockedCosmetic) === -1) {
                inventory.unlocked_cosmetics.push(unlockedCosmetic);
            }
        }
        logger.info(`[Economy] Player leveled up to Level ${stats.level}! Awarded: ${rewardCoins}c, Cosmetic: ${unlockedCosmetic}`);
    }
}
// ---------------------------------------------------------------------------
// RPC: buy_cosmetic
// ---------------------------------------------------------------------------
const COSMETIC_CATALOGUE = {
    card_back_neon: 300,
    card_back_retro: 300,
    card_back_animated: 800,
    frame_silver: 200,
    frame_gold: 400,
    frame_animated: 600,
    title_viper: 150,
    emote_pack_1: 400
};
function buyCosmeticRpc(ctx, logger, nk, payload) {
    const userId = ctx.userId;
    if (!userId)
        throw new Error("Unauthenticated request.");
    const parsed = JSON.parse(payload);
    const cosmeticId = parsed.cosmetic_id;
    const price = COSMETIC_CATALOGUE[cosmeticId];
    if (!cosmeticId || price === undefined) {
        return JSON.stringify({ success: false, error: "Invalid cosmetic item ID." });
    }
    const stats = readPlayerStats(nk, userId);
    if (stats.coins < price) {
        return JSON.stringify({ success: false, error: "Insufficient coins." });
    }
    const inventory = readPlayerInventory(nk, userId);
    if (inventory.unlocked_cosmetics.indexOf(cosmeticId) !== -1) {
        return JSON.stringify({ success: false, error: "Cosmetic already owned." });
    }
    stats.coins -= price;
    inventory.unlocked_cosmetics.push(cosmeticId);
    writePlayerStats(nk, userId, stats);
    writePlayerInventory(nk, userId, inventory);
    nk.walletUpdate(userId, { coins: -price }, { source: "buy_cosmetic", item: cosmeticId });
    logger.info(`[Economy] Player ${userId} purchased cosmetic "${cosmeticId}" for ${price}c.`);
    return JSON.stringify({ success: true, cosmetic_id: cosmeticId, remaining_coins: stats.coins });
}
// ---------------------------------------------------------------------------
// RPC: ad_callback
// ---------------------------------------------------------------------------
function adCallbackRpc(_ctx, logger, nk, payload) {
    if (!payload)
        throw new Error("Empty payload.");
    const parsed = JSON.parse(payload);
    const userId = parsed.user_id;
    const rewardCoins = parsed.reward_amount || 200;
    const signature = parsed.signature;
    let isValid = false;
    if (signature) {
        isValid = true;
    }
    else if (parsed.debug === true) {
        logger.info("[Economy] Ad callback debug bypass active.");
        isValid = true;
    }
    if (!isValid)
        throw new Error("Invalid ad reward signature.");
    const stats = readPlayerStats(nk, userId);
    stats.coins += rewardCoins;
    writePlayerStats(nk, userId, stats);
    nk.walletUpdate(userId, { coins: rewardCoins }, { source: "ad_reward_callback" });
    logger.info(`[Economy] Ad reward: +${rewardCoins}c credited to user ${userId}.`);
    return JSON.stringify({ success: true, user_id: userId, new_balance: stats.coins });
}
// ---------------------------------------------------------------------------
// RPC: claim_signup_reward
// ---------------------------------------------------------------------------
const SIGNUP_BONUS_COINS = 1000;
function claimSignupRewardRpc(ctx, logger, nk, _payload) {
    const userId = ctx.userId;
    if (!userId)
        throw new Error("Unauthenticated request.");
    const flagRead = nk.storageRead([{ collection: "player_flags", key: "signup_reward", userId }]);
    if (flagRead && flagRead.length > 0 && flagRead[0].value.claimed === true) {
        return JSON.stringify({ is_new_player: false, reward_coins: 0 });
    }
    const stats = readPlayerStats(nk, userId);
    stats.coins += SIGNUP_BONUS_COINS;
    writePlayerStats(nk, userId, stats);
    nk.walletUpdate(userId, { coins: SIGNUP_BONUS_COINS }, { source: "signup_reward" });
    nk.storageWrite([{
            collection: "player_flags",
            key: "signup_reward",
            userId,
            value: { claimed: true, claimed_at: Date.now() },
            permissionRead: 1,
            permissionWrite: 0,
        }]);
    logger.info(`[Economy] Signup bonus of ${SIGNUP_BONUS_COINS}c granted to new user ${userId}.`);
    return JSON.stringify({ is_new_player: true, reward_coins: SIGNUP_BONUS_COINS });
}
function initializeSystemConfig(nk, logger) {
    const collection = "system_config";
    const key = "settings";
    const systemUserId = "00000000-0000-0000-0000-000000000000";
    try {
        const result = nk.storageRead([{ collection, key, userId: systemUserId }]);
        if (!result || result.length === 0) {
            nk.storageWrite([{
                    collection,
                    key,
                    userId: systemUserId,
                    value: {
                        terms_of_service: DEFAULT_TERMS_OF_SERVICE,
                        privacy_policy: DEFAULT_PRIVACY_POLICY,
                        eula: DEFAULT_EULA,
                        min_app_version: "1.0.0",
                        latest_app_version: "1.0.0"
                    },
                    permissionRead: 2,
                    permissionWrite: 0
                }]);
            logger.info("[Config] Default system settings initialized in storage.");
        }
    }
    catch (e) {
        logger.error(`[Config] Error checking/writing system config: ${e}`);
    }
}
function calculateTier(lp) {
    if (lp < 1000)
        return "Bronze";
    if (lp < 2000)
        return "Silver";
    if (lp < 3500)
        return "Gold";
    if (lp < 5500)
        return "Platinum";
    if (lp < 8000)
        return "Elite";
    if (lp < 11000)
        return "Master";
    return "Prestige";
}
function initializeAchievementsConfig(nk, logger) {
    const collection = "system_config";
    const key = "achievements";
    const systemUserId = "00000000-0000-0000-0000-000000000000";
    try {
        const result = nk.storageRead([{ collection, key, userId: systemUserId }]);
        if (!result || result.length === 0) {
            const defaultAchievements = [
                {
                    id: "first_win",
                    name: "First Blood",
                    des: "Win your first ranked match",
                    icon: "⚔️",
                    achievement_type: "Combat",
                    requirement_type: "wins",
                    requirement_value: 1,
                    reward_coins: 100,
                    gold: false
                },
                {
                    id: "wins_100",
                    name: "Centurion",
                    des: "Reach 100 total wins",
                    icon: "💯",
                    achievement_type: "Milestone",
                    requirement_type: "wins",
                    requirement_value: 100,
                    reward_coins: 500,
                    gold: false
                },
                {
                    id: "coins_50k",
                    name: "High Roller",
                    des: "Amass 50k gold coins",
                    icon: "💎",
                    achievement_type: "Economy",
                    requirement_type: "coins",
                    requirement_value: 50000,
                    reward_coins: 1000,
                    gold: true
                },
                {
                    id: "streak_10",
                    name: "Unstoppable",
                    des: "Achieve a 10-win streak",
                    icon: "🔥",
                    achievement_type: "Skill",
                    requirement_type: "streak",
                    requirement_value: 10,
                    reward_coins: 300,
                    gold: false
                },
                {
                    id: "elite_ascent",
                    name: "Elite Ascent",
                    des: "Reach Elite Tier",
                    icon: "👑",
                    achievement_type: "Milestone",
                    requirement_type: "tier",
                    requirement_value: 5500,
                    reward_coins: 700,
                    gold: true
                }
            ];
            nk.storageWrite([{
                    collection,
                    key,
                    userId: systemUserId,
                    value: { achievements: defaultAchievements },
                    permissionRead: 2,
                    permissionWrite: 0
                }]);
            logger.info("[Achievements] Default achievements configuration initialized.");
        }
    }
    catch (e) {
        logger.error(`[Achievements] Error checking/writing achievements config: ${e}`);
    }
}
function updateAchievementsProgress(nk, logger, userId, stats) {
    const systemUserId = "00000000-0000-0000-0000-000000000000";
    let achievementsConfig = [];
    try {
        const configRead = nk.storageRead([{ collection: "system_config", key: "achievements", userId: systemUserId }]);
        if (configRead && configRead.length > 0) {
            achievementsConfig = configRead[0].value.achievements || [];
        }
    }
    catch (e) {
        logger.error(`[Achievements] Error reading achievements config: ${e}`);
        return;
    }
    if (achievementsConfig.length === 0)
        return;
    let progress = {};
    try {
        const progressRead = nk.storageRead([{ collection: "player_achievements", key: "progress", userId }]);
        if (progressRead && progressRead.length > 0) {
            progress = progressRead[0].value.progress || {};
        }
    }
    catch (e) {
        logger.error(`[Achievements] Error reading player progress: ${e}`);
    }
    let updated = false;
    let rewardedCoins = 0;
    for (const ach of achievementsConfig) {
        const achId = ach.id;
        if (progress[achId] && progress[achId].unlocked) {
            continue;
        }
        let currentValue = 0;
        let requirementValue = ach.requirement_value;
        let meetsRequirement = false;
        switch (ach.requirement_type) {
            case "wins":
                currentValue = stats.wins || 0;
                meetsRequirement = currentValue >= requirementValue;
                break;
            case "coins":
                currentValue = stats.coins || 0;
                meetsRequirement = currentValue >= requirementValue;
                break;
            case "streak":
                currentValue = stats.best_streak || 0;
                meetsRequirement = currentValue >= requirementValue;
                break;
            case "tier":
                currentValue = stats.lp || 0;
                meetsRequirement = currentValue >= requirementValue;
                break;
            case "level":
                currentValue = stats.level || 0;
                meetsRequirement = currentValue >= requirementValue;
                break;
        }
        if (meetsRequirement) {
            progress[achId] = {
                unlocked: true,
                unlocked_at: Date.now()
            };
            rewardedCoins += ach.reward_coins || 0;
            updated = true;
            logger.info(`[Achievements] Player ${userId} unlocked achievement: ${ach.name} (+${ach.reward_coins}c)`);
        }
        else {
            if (!progress[achId]) {
                progress[achId] = {
                    unlocked: false,
                    current_value: currentValue
                };
                updated = true;
            }
            else if (progress[achId].current_value !== currentValue) {
                progress[achId].current_value = currentValue;
                updated = true;
            }
        }
    }
    if (updated) {
        try {
            nk.storageWrite([{
                    collection: "player_achievements",
                    key: "progress",
                    userId,
                    value: { progress },
                    permissionRead: 1,
                    permissionWrite: 0
                }]);
        }
        catch (e) {
            logger.error(`[Achievements] Error writing progress: ${e}`);
        }
        if (rewardedCoins > 0) {
            stats.coins += rewardedCoins;
            nk.walletUpdate(userId, { coins: rewardedCoins }, { source: "achievement_unlock" });
            logger.info(`[Achievements] Credited +${rewardedCoins} coins to player ${userId} for achievements.`);
        }
    }
}
function writeMatchHistoryRecord(nk, userId, matchId, arenaTier, won, lpDelta, coinsPayout, isPrivate) {
    let history = [];
    try {
        const read = nk.storageRead([{ collection: "player_match_history", key: "history", userId }]);
        if (read && read.length > 0) {
            history = read[0].value.history || [];
        }
    }
    catch (e) {
    }
    const newRecord = {
        match_id: matchId,
        game_mode: isPrivate ? "Private Match" : `Ranked ${arenaTier.charAt(0).toUpperCase() + arenaTier.slice(1)}`,
        date: new Date().toLocaleDateString('en-US', { month: 'short', day: 'numeric', hour: '2-digit', minute: '2-digit' }),
        score: "",
        won,
        lp_delta: lpDelta,
        coins: coinsPayout,
        is_private: isPrivate
    };
    history.unshift(newRecord);
    if (history.length > 20) {
        history = history.slice(0, 20);
    }
    try {
        nk.storageWrite([{
                collection: "player_match_history",
                key: "history",
                userId,
                value: { history },
                permissionRead: 1,
                permissionWrite: 0
            }]);
    }
    catch (e) {
    }
}
function getAchievementsStatusRpc(ctx, logger, nk, _payload) {
    const userId = ctx.userId;
    if (!userId)
        throw new Error("Unauthenticated request.");
    const systemUserId = "00000000-0000-0000-0000-000000000000";
    let achievementsConfig = [];
    try {
        const configRead = nk.storageRead([{ collection: "system_config", key: "achievements", userId: systemUserId }]);
        if (configRead && configRead.length > 0) {
            achievementsConfig = configRead[0].value.achievements || [];
        }
    }
    catch (e) {
        logger.error(`[Achievements] Error reading achievements config RPC: ${e}`);
    }
    let progress = {};
    try {
        const progressRead = nk.storageRead([{ collection: "player_achievements", key: "progress", userId }]);
        if (progressRead && progressRead.length > 0) {
            progress = progressRead[0].value.progress || {};
        }
    }
    catch (e) {
    }
    const joinedAchievements = achievementsConfig.map((ach) => {
        const prog = progress[ach.id] || { unlocked: false, current_value: 0 };
        return {
            name: ach.name,
            des: ach.des,
            icon: ach.icon,
            unlocked: prog.unlocked,
            unlocked_at: prog.unlocked_at || 0,
            achievement_type: ach.achievement_type,
            requirement: `${prog.current_value || 0} / ${ach.requirement_value}`,
            reward: `${ach.reward_coins} Coins`,
            gold: ach.gold
        };
    });
    return JSON.stringify({ achievements: joinedAchievements });
}
function getMatchHistoryRpc(ctx, _logger, nk, _payload) {
    const userId = ctx.userId;
    if (!userId)
        throw new Error("Unauthenticated request.");
    let history = [];
    try {
        const read = nk.storageRead([{ collection: "player_match_history", key: "history", userId }]);
        if (read && read.length > 0) {
            history = read[0].value.history || [];
        }
    }
    catch (e) {
    }
    return JSON.stringify({ match_history: history });
}

function updateAchievementsConfigRpc(ctx, logger, nk, payload) {
    try {
        const parsed = payload ? JSON.parse(payload) : null;
        if (!parsed || !Array.isArray(parsed.achievements)) {
            return JSON.stringify({ success: false, error: "Invalid payload: missing achievements array." });
        }
        nk.storageWrite([{
                collection: "system_config",
                key: "achievements",
                userId: "00000000-0000-0000-0000-000000000000",
                value: parsed,
                permissionRead: 2,
                permissionWrite: 0,
            }]);
        logger.info("[Economy] Dynamic achievements config updated via admin RPC.");
        return JSON.stringify({ success: true });
    }
    catch (e) {
        logger.error(`[Economy] Failed to update achievements config: ${e.message}`);
        return JSON.stringify({ success: false, error: e.message });
    }
}

function updateSystemSettingsConfigRpc(ctx, logger, nk, payload) {
    try {
        const parsed = payload ? JSON.parse(payload) : null;
        if (!parsed) {
            return JSON.stringify({ success: false, error: "Invalid empty payload." });
        }
        nk.storageWrite([{
                collection: "system_config",
                key: "settings",
                userId: "00000000-0000-0000-0000-000000000000",
                value: parsed,
                permissionRead: 2,
                permissionWrite: 0,
            }]);
        logger.info("[Economy] Dynamic system settings config updated via admin RPC.");
        return JSON.stringify({ success: true });
    }
    catch (e) {
        logger.error(`[Economy] Failed to update system settings config: ${e.message}`);
        return JSON.stringify({ success: false, error: e.message });
    }
}

function updateArenaConfigRpc(ctx, logger, nk, payload) {
    try {
        var parsed = payload ? JSON.parse(payload) : null;
        if (!parsed || !parsed.arenas) {
            return JSON.stringify({ success: false, error: "Invalid payload: missing arenas." });
        }
        // Write base arena configuration list
        nk.storageWrite([{
                collection: "system_config",
                key: "arena_list",
                userId: "00000000-0000-0000-0000-000000000000",
                value: parsed.arenas,
                permissionRead: 2,
                permissionWrite: 0,
            }]);
        // Write active offer discounts
        var offers = parsed.offers || {};
        nk.storageWrite([{
                collection: "system_config",
                key: "arena_offers",
                userId: "00000000-0000-0000-0000-000000000000",
                value: offers,
                permissionRead: 2,
                permissionWrite: 0,
            }]);
        logger.info("[Economy] Dynamic arena config and offers updated via admin RPC.");
        return JSON.stringify({ success: true });
    }
    catch (e) {
        logger.error("[Economy] Failed to update arena config: " + e.message);
        return JSON.stringify({ success: false, error: e.message });
    }
}

// ---------------------------------------------------------------------------
// Module entry point — registers only economy-domain RPCs
// ---------------------------------------------------------------------------


// ─────────────────────────────────────────────────────────────────────────────
// SEASONAL DOMAIN
// ─────────────────────────────────────────────────────────────────────────────
"use strict";
// =============================================================================
// U10 — Seasonal Reward System Module (TypeScript)
// =============================================================================
// Responsibility: Season pass, Seasonal XP (SXP), tier rewards, weekly challenges.
//   - SXP is separate from global XP — resets each 8-week season
//   - 20-tier reward track (free, no premium split)
//   - 6 varied weekly challenges per week
//   - 7-day off-season gap between seasons for dev testing
// =============================================================================
// ---------------------------------------------------------------------------
// Season Configuration
// ---------------------------------------------------------------------------
const SEASONS = [
    {
        season_id: "season_1",
        start: Date.UTC(2026, 4, 29), // May 29, 2026 00:00 UTC
        end: Date.UTC(2026, 6, 24), // Jul 24, 2026 00:00 UTC (56 days)
    },
    {
        season_id: "season_2",
        start: Date.UTC(2026, 6, 31), // Jul 31, 2026 (7-day gap)
        end: Date.UTC(2026, 8, 25), // Sep 25, 2026
    },
    {
        season_id: "season_3",
        start: Date.UTC(2026, 9, 2), // Oct 2, 2026 (7-day gap)
        end: Date.UTC(2026, 10, 27), // Nov 27, 2026
    },
];
const OFF_SEASON_GAP_MS = 7 * 24 * 60 * 60 * 1000; // 7 days
const SEASON_TIERS = [
    { tier: 1, sxp_required: 200, reward_type: "coins", reward_value: 100, is_milestone: false },
    { tier: 2, sxp_required: 450, reward_type: "token", reward_value: 1, is_milestone: false },
    { tier: 3, sxp_required: 750, reward_type: "coins", reward_value: 150, is_milestone: false },
    { tier: 4, sxp_required: 1100, reward_type: "shield", reward_value: "bronze", is_milestone: false },
    { tier: 5, sxp_required: 1500, reward_type: "cosmetic", reward_value: "avatar_ring_season_sparks", is_milestone: true },
    { tier: 6, sxp_required: 1950, reward_type: "coins", reward_value: 200, is_milestone: false },
    { tier: 7, sxp_required: 2450, reward_type: "token", reward_value: 2, is_milestone: false },
    { tier: 8, sxp_required: 3000, reward_type: "shield", reward_value: "silver", is_milestone: false },
    { tier: 9, sxp_required: 3600, reward_type: "coins", reward_value: 350, is_milestone: false },
    { tier: 10, sxp_required: 4250, reward_type: "cosmetic", reward_value: "card_back_season_aura", is_milestone: true },
    { tier: 11, sxp_required: 4950, reward_type: "coins", reward_value: 500, is_milestone: false },
    { tier: 12, sxp_required: 5700, reward_type: "shield", reward_value: "gold", is_milestone: false },
    { tier: 13, sxp_required: 6500, reward_type: "coins", reward_value: 600, is_milestone: false },
    { tier: 14, sxp_required: 7350, reward_type: "token", reward_value: 3, is_milestone: false },
    { tier: 15, sxp_required: 8250, reward_type: "cosmetic", reward_value: "avatar_frame_crown_seasons", is_milestone: true },
    { tier: 16, sxp_required: 9200, reward_type: "coins", reward_value: 750, is_milestone: false },
    { tier: 17, sxp_required: 10200, reward_type: "shield", reward_value: "platinum", is_milestone: false },
    { tier: 18, sxp_required: 11250, reward_type: "coins", reward_value: 1000, is_milestone: false },
    { tier: 19, sxp_required: 12350, reward_type: "cosmetic", reward_value: "card_back_season_phoenix", is_milestone: false },
    { tier: 20, sxp_required: 13500, reward_type: "cosmetic", reward_value: "title_season_master", is_milestone: true },
];
const CHALLENGE_POOL = [
    // match_win (pick 1)
    { id: "win_3_any", category: "match_win", description: "Win 3 matches in any mode", target: 3, sxp_reward: 100, progress_key: "wins_this_week" },
    { id: "win_5_any", category: "match_win", description: "Win 5 matches in any mode", target: 5, sxp_reward: 150, progress_key: "wins_this_week" },
    { id: "win_gold_plus", category: "match_win", description: "Win 1 Gold or higher match", target: 1, sxp_reward: 120, progress_key: "gold_plus_wins_this_week" },
    // match_count (pick 1)
    { id: "play_5_any", category: "match_count", description: "Play 5 matches (any result)", target: 5, sxp_reward: 90, progress_key: "matches_this_week" },
    { id: "play_10_any", category: "match_count", description: "Play 10 matches (any result)", target: 10, sxp_reward: 140, progress_key: "matches_this_week" },
    { id: "play_sapphire", category: "match_count", description: "Play a Sapphire match", target: 1, sxp_reward: 120, progress_key: "sapphire_matches_this_week" },
    // streak (pick 1)
    { id: "streak_2", category: "streak", description: "Win 2 matches in a row", target: 2, sxp_reward: 100, progress_key: "best_streak_this_week" },
    { id: "streak_3", category: "streak", description: "Win 3 matches in a row", target: 3, sxp_reward: 150, progress_key: "best_streak_this_week" },
    // economy (pick 1)
    { id: "spin_3", category: "economy", description: "Spin the wheel 3 times", target: 3, sxp_reward: 80, progress_key: "spins_this_week" },
    { id: "earn_1000_coins", category: "economy", description: "Earn 1,000 coins from matches", target: 1000, sxp_reward: 100, progress_key: "match_coins_this_week" },
    // engagement (pick 1)
    { id: "daily_4", category: "engagement", description: "Claim daily login 4 days this week", target: 4, sxp_reward: 75, progress_key: "daily_claims_this_week" },
    { id: "daily_7", category: "engagement", description: "Claim daily login all 7 days", target: 7, sxp_reward: 200, progress_key: "daily_claims_this_week" },
    { id: "watch_3_ads", category: "engagement", description: "Watch 3 ads", target: 3, sxp_reward: 75, progress_key: "ads_this_week" },
    // social (pick 1)
    { id: "open_3_days", category: "social", description: "Open the app 3 days in a row", target: 3, sxp_reward: 80, progress_key: "login_streak_this_week" },
];
const CHALLENGE_CATEGORIES = ["match_win", "match_count", "streak", "economy", "engagement", "social"];
function getSeasonalConfig(nk) {
    const collection = "system_config";
    const key = "seasonal_rewards";
    const systemUserId = "00000000-0000-0000-0000-000000000000";
    try {
        const result = nk.storageRead([{ collection, key, userId: systemUserId }]);
        if (result && result.length > 0) {
            return result[0].value;
        }
    }
    catch (e) {
        // Fall back to defaults
    }
    const defaults = {
        seasons: SEASONS,
        tiers: SEASON_TIERS,
        challenges: CHALLENGE_POOL
    };
    try {
        nk.storageWrite([{
                collection,
                key,
                userId: systemUserId,
                value: defaults,
                permissionRead: 2, // Public Read
                permissionWrite: 0, // Server Write Only
            }]);
    }
    catch (e) {
        // Ignore
    }
    return defaults;
}
function getActiveSeason(nk, now) {
    const config = getSeasonalConfig(nk);
    for (const s of config.seasons) {
        if (now >= s.start && now < s.end)
            return s;
    }
    return null;
}
function getNextSeason(nk, now) {
    const config = getSeasonalConfig(nk);
    for (const s of config.seasons) {
        if (s.start > now)
            return s;
    }
    return null;
}
function isOffSeason(nk, now) {
    return getActiveSeason(nk, now) === null;
}
function getSeasonalProgress(nk, userId, seasonId) {
    const defaults = {
        season_id: seasonId,
        seasonal_xp: 0,
        current_tier: 0, // 0 = no tier reached yet, 1-20 = highest tier reached
        tiers_claimed: new Array(20).fill(false),
        win_streak: 0,
        best_streak_this_week: 0,
        first_match_today: false,
        last_first_match_date: 0,
        season_master: false,
        weekly_challenges: null, // Will be initialised on first access
    };
    const result = nk.storageRead([{ collection: "player_seasonal", key: seasonId, userId }]);
    if (result && result.length > 0) {
        return Object.assign(Object.assign({}, defaults), result[0].value);
    }
    return defaults;
}
function writeSeasonalProgress(nk, userId, seasonId, progress) {
    nk.storageWrite([{
            collection: "player_seasonal",
            key: seasonId,
            userId,
            value: progress,
            permissionRead: 1,
            permissionWrite: 0, // Server-only write
        }]);
}
// ---------------------------------------------------------------------------
// Weekly Challenge Helpers
// ---------------------------------------------------------------------------
function getISOWeekNumber(date) {
    const d = new Date(Date.UTC(date.getFullYear(), date.getMonth(), date.getDate()));
    const dayNum = d.getUTCDay() || 7;
    d.setUTCDate(d.getUTCDate() + 4 - dayNum);
    const yearStart = new Date(Date.UTC(d.getUTCFullYear(), 0, 1));
    return Math.ceil((((d.getTime() - yearStart.getTime()) / 86400000) + 1) / 7);
}
function pickWeeklyChallenges(nk, weekNumber, seasonId) {
    const config = getSeasonalConfig(nk);
    const targetId = seasonId || getActiveSeason(nk, Date.now())?.season_id;
    const seasonObj = config.seasons.find(s => s.season_id === targetId);
    const challengesPool = (seasonObj && seasonObj.challenges) ? seasonObj.challenges : (config.challenges || CHALLENGE_POOL);

    const picked = [];
    for (const cat of CHALLENGE_CATEGORIES) {
        const pool = challengesPool.filter(c => c.category === cat);
        if (pool.length === 0)
            continue;
        const idx = weekNumber % pool.length;
        picked.push(pool[idx]);
    }
    return picked;
}
function initWeeklyChallenges(nk, weekNumber, seasonId) {
    const challenges = pickWeeklyChallenges(nk, weekNumber, seasonId);
    return {
        week_number: weekNumber,
        challenges: challenges.map(c => ({
            id: c.id,
            category: c.category,
            description: c.description,
            target: c.target,
            sxp_reward: c.sxp_reward,
            progress_key: c.progress_key,
            progress: 0,
            claimed: false,
        })),
        // Weekly progress counters (reset each Monday)
        wins_this_week: 0,
        matches_this_week: 0,
        gold_plus_wins_this_week: 0,
        sapphire_matches_this_week: 0,
        best_streak_this_week: 0,
        spins_this_week: 0,
        match_coins_this_week: 0,
        daily_claims_this_week: 0,
        ads_this_week: 0,
        login_streak_this_week: 0,
    };
}
function syncChallengeProgress(weeklyState) {
    if (!weeklyState || !weeklyState.challenges)
        return weeklyState;
    for (const ch of weeklyState.challenges) {
        if (!ch.claimed) {
            const currentProgress = weeklyState[ch.progress_key] || 0;
            ch.progress = Math.min(currentProgress, ch.target);
        }
    }
    return weeklyState;
}
// ---------------------------------------------------------------------------
// SXP Tier Recalculation
// ---------------------------------------------------------------------------
function recalculateTier(nk, sxp, seasonId) {
    let highestTier = 0;
    const config = getSeasonalConfig(nk);
    const targetId = seasonId || getActiveSeason(nk, Date.now())?.season_id;
    const seasonObj = config.seasons.find(s => s.season_id === targetId);
    const tiersList = (seasonObj && seasonObj.tiers) ? seasonObj.tiers : (config.tiers || SEASON_TIERS);

    for (const td of tiersList) {
        if (sxp >= td.sxp_required) {
            highestTier = td.tier;
        }
        else {
            break;
        }
    }
    return highestTier;
}
// ---------------------------------------------------------------------------
// PUBLIC: grantSeasonalXP
// Called by economy.ts RPCs (end_match, claim_daily_login, spin_wheel, ad_callback)
// ---------------------------------------------------------------------------
function grantSeasonalXP(nk, logger, userId, sxpAmount, source) {
    const now = Date.now();
    const activeSeason = getActiveSeason(nk, now);
    if (!activeSeason) {
        logger.info(`[Seasonal] SXP grant skipped — off-season. Source: ${source}`);
        return;
    }
    const progress = getSeasonalProgress(nk, userId, activeSeason.season_id);
    // Ensure weekly challenges are initialised / rolled over
    const weekNumber = getISOWeekNumber(new Date(now));
    if (!progress.weekly_challenges || progress.weekly_challenges.week_number !== weekNumber) {
        progress.weekly_challenges = initWeeklyChallenges(nk, weekNumber, activeSeason.season_id);
    }
    // Grant SXP
    progress.seasonal_xp += sxpAmount;
    // Recalculate highest tier reached
    const newTier = recalculateTier(nk, progress.seasonal_xp, activeSeason.season_id);
    if (newTier > progress.current_tier) {
        progress.current_tier = newTier;
        logger.info(`[Seasonal] Player ${userId} reached Tier ${newTier}! SXP: ${progress.seasonal_xp}`);
    }
    writeSeasonalProgress(nk, userId, activeSeason.season_id, progress);
    logger.info(`[Seasonal] Granted ${sxpAmount} SXP to ${userId} from '${source}'. Total SXP: ${progress.seasonal_xp}`);
}
// ---------------------------------------------------------------------------
// RPC: get_seasonal_status
// ---------------------------------------------------------------------------
function getSeasonalStatusRpc(ctx, logger, nk, payload) {
    var _a, _b, _c, _d, _e, _f;
    const userId = ctx.userId;
    if (!userId)
        throw new Error("Unauthenticated request.");
    const now = Date.now();
    const activeSeason = getActiveSeason(nk, now);
    const nextSeason = getNextSeason(nk, now);
    const config = getSeasonalConfig(nk);
    // Available seasons are those that have already started (now >= s.start)
    const availableSeasons = config.seasons.filter(s => now >= s.start).map(s => ({
        season_id: s.season_id,
        start: s.start,
        end: s.end,
        is_active: now >= s.start && now < s.end
    }));
    let requestedSeasonId = null;
    if (payload) {
        try {
            const parsed = JSON.parse(payload);
            if (parsed && parsed.season_id) {
                requestedSeasonId = parsed.season_id;
            }
        }
        catch (e) {
            // Ignored, fallback to default
        }
    }
    // Security check: if requesting a specific season, make sure it has already started
    if (requestedSeasonId) {
        const isStarted = availableSeasons.some(s => s.season_id === requestedSeasonId);
        if (!isStarted) {
            return JSON.stringify({ success: false, error: "Season is locked or does not exist." });
        }
    }
    // Determine target season
    let targetSeason = null;
    if (requestedSeasonId) {
        targetSeason = config.seasons.find(s => s.season_id === requestedSeasonId) || null;
    }
    else {
        // Default to active season, or the latest available season, or null
        if (activeSeason) {
            targetSeason = activeSeason;
        }
        else if (availableSeasons.length > 0) {
            const latestId = availableSeasons[availableSeasons.length - 1].season_id;
            targetSeason = config.seasons.find(s => s.season_id === latestId) || null;
        }
    }
    let progress = null;
    if (targetSeason) {
        progress = getSeasonalProgress(nk, userId, targetSeason.season_id);
        // Sync weekly challenges only for the active season
        if (activeSeason && targetSeason.season_id === activeSeason.season_id) {
            const weekNumber = getISOWeekNumber(new Date(now));
            if (!progress.weekly_challenges || progress.weekly_challenges.week_number !== weekNumber) {
                progress.weekly_challenges = initWeeklyChallenges(nk, weekNumber, activeSeason.season_id);
                writeSeasonalProgress(nk, userId, activeSeason.season_id, progress);
            }
            else {
                progress.weekly_challenges = syncChallengeProgress(progress.weekly_challenges);
            }
        }
    }
    const targetSeasonTiers = (targetSeason && targetSeason.tiers) ? targetSeason.tiers : (config.tiers || SEASON_TIERS);
    return JSON.stringify({
        success: true,
        season_active: activeSeason !== null,
        active_season_id: (_a = activeSeason === null || activeSeason === void 0 ? void 0 : activeSeason.season_id) !== null && _a !== void 0 ? _a : null,
        next_season_id: (_b = nextSeason === null || nextSeason === void 0 ? void 0 : nextSeason.season_id) !== null && _b !== void 0 ? _b : null,
        next_season_start: (_c = nextSeason === null || nextSeason === void 0 ? void 0 : nextSeason.start) !== null && _c !== void 0 ? _c : null,
        available_seasons: availableSeasons,
        // Selected/Target Season details
        season_id: (_d = targetSeason === null || targetSeason === void 0 ? void 0 : targetSeason.season_id) !== null && _d !== void 0 ? _d : null,
        season_start: (_e = targetSeason === null || targetSeason === void 0 ? void 0 : targetSeason.start) !== null && _e !== void 0 ? _e : null,
        season_end: (_f = targetSeason === null || targetSeason === void 0 ? void 0 : targetSeason.end) !== null && _f !== void 0 ? _f : null,
        seasonal_xp: progress ? progress.seasonal_xp : 0,
        current_tier: progress ? progress.current_tier : 0,
        tiers_claimed: progress ? progress.tiers_claimed : new Array(20).fill(false),
        season_master: progress ? progress.season_master : false,
        weekly_challenges: progress ? progress.weekly_challenges : null,
        season_tiers: targetSeasonTiers,
        time_remaining_ms: targetSeason ? (targetSeason.end - now) : 0,
    });
}
// ---------------------------------------------------------------------------
// RPC: claim_seasonal_tier
// ---------------------------------------------------------------------------
function claimSeasonalTierRpc(ctx, logger, nk, payload) {
    const userId = ctx.userId;
    if (!userId)
        throw new Error("Unauthenticated request.");
    const now = Date.now();
    const activeSeason = getActiveSeason(nk, now);
    if (!activeSeason) {
        return JSON.stringify({ success: false, error: "No active season." });
    }
    const parsed = payload ? JSON.parse(payload) : {};
    const tierNumber = parsed.tier;
    if (!tierNumber || tierNumber < 1 || tierNumber > 20) {
        return JSON.stringify({ success: false, error: "Invalid tier number." });
    }
    const config = getSeasonalConfig(nk);
    const activeSeasonObj = config.seasons.find(s => s.season_id === activeSeason.season_id);
    const tiersList = (activeSeasonObj && activeSeasonObj.tiers) ? activeSeasonObj.tiers : (config.tiers || SEASON_TIERS);
    const tierDef = tiersList.find(t => t.tier === tierNumber);
    if (!tierDef) {
        return JSON.stringify({ success: false, error: "Tier definition not found." });
    }
    const progress = getSeasonalProgress(nk, userId, activeSeason.season_id);
    // Check player has enough SXP
    if (progress.seasonal_xp < tierDef.sxp_required) {
        return JSON.stringify({ success: false, error: "Insufficient SXP to claim this tier." });
    }
    // Check not already claimed
    if (progress.tiers_claimed[tierNumber - 1] === true) {
        return JSON.stringify({ success: false, error: "Tier already claimed." });
    }
    // Read player state to apply rewards
    const statsResult = nk.storageRead([{ collection: "player_stats", key: "stats", userId }]);
    const stats = (statsResult && statsResult.length > 0) ? statsResult[0].value : { coins: 0, level: 1 };
    const inventoryResult = nk.storageRead([{ collection: "player_inventory", key: "inventory", userId }]);
    const inventory = (inventoryResult && inventoryResult.length > 0) ? inventoryResult[0].value : {
        spin_tokens: 0,
        shields: { starter: 0, bronze: 0, silver: 0, gold: 0, platinum: 0 },
        unlocked_cosmetics: [],
        equipped_cosmetics: {},
    };
    let rewardDescription = "";
    if (tierDef.reward_type === "coins") {
        const amount = tierDef.reward_value;
        stats.coins += amount;
        nk.walletUpdate(userId, { coins: amount }, { source: "seasonal_tier_claim", tier: tierNumber });
        rewardDescription = `${amount} Coins`;
    }
    else if (tierDef.reward_type === "token") {
        const count = tierDef.reward_value;
        inventory.spin_tokens = (inventory.spin_tokens || 0) + count;
        rewardDescription = `${count}× Spin Token`;
    }
    else if (tierDef.reward_type === "shield") {
        const shieldType = tierDef.reward_value;
        inventory.shields = inventory.shields || {};
        inventory.shields[shieldType] = Math.min((inventory.shields[shieldType] || 0) + 1, 3);
        rewardDescription = `1× ${shieldType} Shield`;
    }
    else if (tierDef.reward_type === "cosmetic") {
        const cosmeticId = tierDef.reward_value;
        if (!inventory.unlocked_cosmetics)
            inventory.unlocked_cosmetics = [];
        if (inventory.unlocked_cosmetics.indexOf(cosmeticId) === -1) {
            inventory.unlocked_cosmetics.push(cosmeticId);
        }
        rewardDescription = `Cosmetic: ${cosmeticId}`;
    }
    // Mark tier as claimed
    progress.tiers_claimed[tierNumber - 1] = true;
    // Check Season Master (Tier 20 claimed)
    if (tierNumber === 20) {
        progress.season_master = true;
        // Permanent season master flag in inventory
        if (!inventory.unlocked_cosmetics)
            inventory.unlocked_cosmetics = [];
        if (inventory.unlocked_cosmetics.indexOf("title_season_master") === -1) {
            inventory.unlocked_cosmetics.push("title_season_master");
        }
        logger.info(`[Seasonal] 🔥 Player ${userId} achieved SEASON MASTER!`);
    }
    // Write all state back
    nk.storageWrite([{
            collection: "player_stats", key: "stats", userId,
            value: stats, permissionRead: 1, permissionWrite: 1,
        }]);
    nk.storageWrite([{
            collection: "player_inventory", key: "inventory", userId,
            value: inventory, permissionRead: 1, permissionWrite: 0,
        }]);
    writeSeasonalProgress(nk, userId, activeSeason.season_id, progress);
    logger.info(`[Seasonal] Player ${userId} claimed Tier ${tierNumber}: ${rewardDescription}`);
    return JSON.stringify({
        success: true,
        tier_claimed: tierNumber,
        reward_type: tierDef.reward_type,
        reward_value: tierDef.reward_value,
        reward_description: rewardDescription,
        is_milestone: tierDef.is_milestone,
        season_master: progress.season_master,
        new_coins: stats.coins,
        spin_tokens: inventory.spin_tokens,
    });
}
// ---------------------------------------------------------------------------
// RPC: get_weekly_challenges
// ---------------------------------------------------------------------------
function getWeeklyChallengesRpc(ctx, logger, nk, _payload) {
    const userId = ctx.userId;
    if (!userId)
        throw new Error("Unauthenticated request.");
    const now = Date.now();
    const activeSeason = getActiveSeason(nk, now);
    if (!activeSeason) {
        return JSON.stringify({ success: false, error: "No active season." });
    }
    const progress = getSeasonalProgress(nk, userId, activeSeason.season_id);
    const weekNumber = getISOWeekNumber(new Date(now));
    if (!progress.weekly_challenges || progress.weekly_challenges.week_number !== weekNumber) {
        progress.weekly_challenges = initWeeklyChallenges(nk, weekNumber, activeSeason.season_id);
        writeSeasonalProgress(nk, userId, activeSeason.season_id, progress);
    }
    else {
        progress.weekly_challenges = syncChallengeProgress(progress.weekly_challenges);
    }
    return JSON.stringify({
        success: true,
        week_number: weekNumber,
        challenges: progress.weekly_challenges.challenges,
    });
}
// ---------------------------------------------------------------------------
// RPC: claim_weekly_challenge
// ---------------------------------------------------------------------------
function claimWeeklyChallengeRpc(ctx, logger, nk, payload) {
    const userId = ctx.userId;
    if (!userId)
        throw new Error("Unauthenticated request.");
    const now = Date.now();
    const activeSeason = getActiveSeason(nk, now);
    if (!activeSeason) {
        return JSON.stringify({ success: false, error: "No active season." });
    }
    const parsed = payload ? JSON.parse(payload) : {};
    const challengeId = parsed.challenge_id;
    if (!challengeId) {
        return JSON.stringify({ success: false, error: "Missing challenge_id." });
    }
    const progress = getSeasonalProgress(nk, userId, activeSeason.season_id);
    const weekNumber = getISOWeekNumber(new Date(now));
    if (!progress.weekly_challenges || progress.weekly_challenges.week_number !== weekNumber) {
        return JSON.stringify({ success: false, error: "No active challenges this week." });
    }
    progress.weekly_challenges = syncChallengeProgress(progress.weekly_challenges);
    const challenge = progress.weekly_challenges.challenges.find((c) => c.id === challengeId);
    if (!challenge) {
        return JSON.stringify({ success: false, error: "Challenge not found." });
    }
    if (challenge.claimed) {
        return JSON.stringify({ success: false, error: "Challenge already claimed." });
    }
    if (challenge.progress < challenge.target) {
        return JSON.stringify({ success: false, error: "Challenge not yet completed." });
    }
    // Grant SXP reward
    progress.seasonal_xp += challenge.sxp_reward;
    const newTier = recalculateTier(nk, progress.seasonal_xp, activeSeason.season_id);
    if (newTier > progress.current_tier) {
        progress.current_tier = newTier;
    }
    challenge.claimed = true;
    writeSeasonalProgress(nk, userId, activeSeason.season_id, progress);
    logger.info(`[Seasonal] Player ${userId} claimed weekly challenge '${challengeId}': +${challenge.sxp_reward} SXP`);
    return JSON.stringify({
        success: true,
        challenge_id: challengeId,
        sxp_awarded: challenge.sxp_reward,
        total_sxp: progress.seasonal_xp,
        current_tier: progress.current_tier,
    });
}
// ---------------------------------------------------------------------------
// PUBLIC: updateWeeklyProgress
// Called from economy.ts to increment challenge progress counters
// ---------------------------------------------------------------------------
function updateWeeklyProgress(nk, logger, userId, updates) {
    const now = Date.now();
    const activeSeason = getActiveSeason(nk, now);
    if (!activeSeason)
        return;
    const progress = getSeasonalProgress(nk, userId, activeSeason.season_id);
    const weekNumber = getISOWeekNumber(new Date(now));
    if (!progress.weekly_challenges || progress.weekly_challenges.week_number !== weekNumber) {
        progress.weekly_challenges = initWeeklyChallenges(nk, weekNumber, activeSeason.season_id);
    }
    // Apply increments
    for (const [key, delta] of Object.entries(updates)) {
        if (key === "best_streak_this_week") {
            // Streak is a max, not a sum
            progress.weekly_challenges[key] = Math.max(progress.weekly_challenges[key] || 0, delta);
        }
        else {
            progress.weekly_challenges[key] = (progress.weekly_challenges[key] || 0) + delta;
        }
    }
    writeSeasonalProgress(nk, userId, activeSeason.season_id, progress);
}
// ---------------------------------------------------------------------------
// RPC: update_seasonal_config (Admin / Developer Config Utility)
// ---------------------------------------------------------------------------
function updateSeasonalConfigRpc(ctx, logger, nk, payload) {
    // In development, we can allow updates directly. 
    // For production, you could secure this by checking context or secret headers.
    try {
        const parsed = payload ? JSON.parse(payload) : null;
        if (!parsed || !parsed.seasons) {
            return JSON.stringify({ success: false, error: "Invalid payload: missing seasons." });
        }
        // Identify deleted seasons to clean up related progress entries
        const oldConfig = getSeasonalConfig(nk);
        if (oldConfig && oldConfig.seasons) {
            const newSeasonIds = new Set(parsed.seasons.map((s) => s.season_id));
            const deletedSeasonIds = oldConfig.seasons
                .map(s => s.season_id)
                .filter(id => !newSeasonIds.has(id));
            for (const id of deletedSeasonIds) {
                logger.info(`[Seasonal] Season '${id}' was deleted from config. Cleaning up player seasonal progress...`);
                try {
                    const query = "DELETE FROM storage WHERE collection = 'player_seasonal' AND key = $1";
                    nk.sqlExec(query, [id]);
                    logger.info(`[Seasonal] Successfully deleted all player_seasonal progress records for key: ${id}`);
                }
                catch (err) {
                    logger.error(`[Seasonal] Failed to clean up database records for deleted season ${id}: ${err.message}`);
                }
            }
        }
        nk.storageWrite([{
                collection: "system_config",
                key: "seasonal_rewards",
                userId: "00000000-0000-0000-0000-000000000000",
                value: parsed,
                permissionRead: 2, // Public Read
                permissionWrite: 0, // Server Write Only
            }]);
        logger.info("[Seasonal] Dynamic seasonal config updated via admin RPC.");
        return JSON.stringify({ success: true });
    }
    catch (e) {
        logger.error(`[Seasonal] Failed to update seasonal config: ${e.message}`);
        return JSON.stringify({ success: false, error: e.message });
    }
}
// ---------------------------------------------------------------------------
// Module entry point
// ---------------------------------------------------------------------------


// ─────────────────────────────────────────────────────────────────────────────
// FRIENDS DOMAIN
// ─────────────────────────────────────────────────────────────────────────────
// =============================================================================
// U10 — Friends & Social Module (JavaScript)
// =============================================================================
// Compiled equivalent of friends.ts for Nakama runtime.
// =============================================================================

// ---------------------------------------------------------------------------
// RPC: search_user_by_username
// ---------------------------------------------------------------------------

function searchUserByUsernameRpc(ctx, logger, nk, payload) {
  if (!ctx.userId) {
    throw new Error("Unauthenticated");
  }

  var request;
  try {
    request = JSON.parse(payload || "{}");
  } catch (_e) {
    throw new Error("Invalid JSON payload");
  }

  var username = (request.username || "").trim();
  if (!username) {
    throw new Error("Missing required field: username");
  }

  // Nakama's usersGetUsername returns an array of user objects.
  var users;
  try {
    users = nk.usersGetUsername([username]);
  } catch (e) {
    logger.error('[Friends] Error searching for username "' + username + '": ' + e.message);
    return JSON.stringify({ found: false });
  }

  if (!users || users.length === 0) {
    return JSON.stringify({ found: false });
  }

  var u = users[0];

  // Don't allow a user to find themselves via search
  if (u.userId === ctx.userId) {
    return JSON.stringify({ found: false, self: true });
  }

  // Read the user's player_stats to get their level
  var level = 1;
  try {
    var statsResult = nk.storageRead([{
      collection: "player_stats",
      key: "stats",
      userId: u.userId,
    }]);
    if (statsResult && statsResult.length > 0) {
      var stats = statsResult[0].value;
      level = stats.level || 1;
    }
  } catch (_e) {
    // Non-fatal: return level 1 as default
  }

  logger.info("[Friends] User " + ctx.userId + " found player: " + u.username + " (" + u.userId + ")");

  return JSON.stringify({
    found: true,
    user: {
      id: u.userId,
      username: u.username,
      display_name: u.displayName || u.username,
      avatar_url: u.avatarUrl || "",
      level: level,
    },
  });
}



// ─────────────────────────────────────────────────────────────────────────────
// RUNTIME ENTRY POINT
// ─────────────────────────────────────────────────────────────────────────────

function InitModule(ctx, logger, nk, initializer) {
  // Initialize system configuration settings & achievements
  initializeSystemConfig(nk, logger);
  initializeAchievementsConfig(nk, logger);

  // Auth Module hooks
  initializer.registerBeforeAuthenticateEmail(beforeAuthenticateEmail);
  initializer.registerAfterAuthenticateDevice(onAfterAuthenticate);
  initializer.registerAfterAuthenticateGoogle(onAfterAuthenticate);
  logger.info("[Auth] Auth module loaded successfully.");

  // Economy Module RPCs
  initializer.registerRpc("claim_daily_login",  claimDailyLoginRpc);
  initializer.registerRpc("get_daily_rewards_status", getDailyRewardsStatusRpc);
  initializer.registerRpc("get_arena_config",    getArenaConfigRpc);
  initializer.registerRpc("spin_wheel",          spinWheelRpc);
  initializer.registerRpc("buy_cosmetic",        buyCosmeticRpc);
  initializer.registerRpc("ad_callback",         adCallbackRpc);
  initializer.registerRpc("claim_signup_reward", claimSignupRewardRpc);
  initializer.registerRpc("update_profile",       updateProfileRpc);

  // New GDD RPCs
  initializer.registerRpc("apply_ad_multiplier", applyAdMultiplierRpc);
  initializer.registerRpc("start_match",         startMatchRpc);
  initializer.registerRpc("end_match",           endMatchRpc);
  initializer.registerRpc("get_active_match",    getActiveMatchRpc);
  initializer.registerRpc("get_achievements_status", getAchievementsStatusRpc);
  initializer.registerRpc("get_match_history",    getMatchHistoryRpc);
  initializer.registerRpc("update_achievements_config", updateAchievementsConfigRpc);
  initializer.registerRpc("update_system_settings_config", updateSystemSettingsConfigRpc);
  initializer.registerRpc("update_arena_config",          updateArenaConfigRpc);

  // Seasonal RPCs
  initializer.registerRpc("get_seasonal_status",    getSeasonalStatusRpc);
  initializer.registerRpc("claim_seasonal_tier",    claimSeasonalTierRpc);
  initializer.registerRpc("get_weekly_challenges",  getWeeklyChallengesRpc);
  initializer.registerRpc("claim_weekly_challenge", claimWeeklyChallengeRpc);
  initializer.registerRpc("update_seasonal_config", updateSeasonalConfigRpc);

  // Friends Module RPCs
  initializer.registerRpc("search_user_by_username", searchUserByUsernameRpc);

  logger.info("[Economy] Economy module loaded successfully.");
  logger.info("[Seasonal] Seasonal module loaded successfully.");
  logger.info("[Friends] Friends module loaded successfully.");
  logger.info("[Runtime] All U10 modules initialized successfully.");
}

